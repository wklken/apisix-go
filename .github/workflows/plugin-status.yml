name: Plugin Status Contract

on:
  pull_request:
  push:
    branches:
      - master
    paths:
      - Makefile
      - docs/plugins.md
      - scripts/plugin_status_gate_test.sh
      - t/plugin/*.yaml
      - t/plugin/coverage_test.go
      - .github/workflows/plugin-status.yml
  workflow_dispatch:

permissions:
  contents: read

jobs:
  plugin-status:
    name: Plugin Status Contract
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Set up Go
        uses: actions/setup-go@v7
        with:
          go-version-file: go.mod
          cache: true

      - name: Verify supported plugin manifest selection
        run: make test-plugin-status
