name: CI

on:
  pull_request:
    paths:
      - '**'
      - '!**/*.md'
      - docs/plugins.md
      - 'docs/architecture/**'
  push:
    branches:
      - master
    paths:
      - '**'
      - '!**/*.md'
      - docs/plugins.md
      - 'docs/architecture/**'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Set up Go
        uses: actions/setup-go@v7
        with:
          go-version-file: go.mod
          cache: true

      - name: Run lint
        uses: golangci/golangci-lint-action@v9.3.0
        with:
          version: v2.12.2

  build-and-unit:
    name: Build And Unit Coverage
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Verify CI, capability, and candidate workflow contracts
        run: bash scripts/release_gate_test.sh

      - name: Set up Go
        uses: actions/setup-go@v7
        with:
          go-version-file: go.mod
          cache: true

      - name: Verify plugin behavior gate contract
        run: make test-plugin-behavior-gate

      - name: Verify generated capability and ADR drift
        run: make check-capability-drift

      - name: Build
        run: make build

      - name: Run source unit tests and coverage gate
        run: make test-cover

      - name: Run plugin harness unit tests
        run: make test-plugin-harness
