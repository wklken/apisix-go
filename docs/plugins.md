# APISIX 3.17 Plugin Support

> Current Go-native plugin status, supported behavior, and remaining compatibility boundaries.
>
> Official comparison baseline: Apache APISIX `release/3.17` (104 default plugins). Evidence is maintained against `pkg/plugin/init.go`, the plugin packages, route/proxy integration, focused tests, and the repository verification gate.
>
> Status audit: 2026-08-06.

## Summary

- Official APISIX 3.17 default plugins: **104**
- Registered default plugins: **100** (**96.2%**)
- Supported at the documented Go-native monitoring level: **89** (**85.6%**)
- Deferred because of native/runtime or separate-subsystem boundaries: **9**
- Unsupported or intentionally not required in normal parity: **6**
- Additional documented rows: **14** (12 README/native extras and 2 registered non-default protocol entries)
- Total plugin rows in the status table: **118**

## Integrated plugin tests

The standalone integration suite under [`t/plugin`](../t/plugin/README.md) is
pinned to Apache APISIX commit
`c3d7d5ec69774121f53d2e20d29d09c816795dd7`. It contains 99 manifest files for
the 98 source-backed plugins marked Supported here plus supplemental
`redirect2`. Every manifest activates its target plugin in standalone resources
and passes the semantic coverage gate. Every scenario writes fresh
`config.yaml` and `apisix.yaml`, starts the real APISIX-Go CLI in standalone
YAML-provider mode, sends requests, and asserts the response, fixture request,
or startup log. External services are replaced by deterministic per-case
loopback fixtures, including TCP/TLS-TCP, UDP, gRPC, Redis, Kafka, Dubbo, and
LDAP protocol fixtures; no placeholder or skip reason is treated as coverage.

The four intentionally unregistered defaults are `ext-plugin-pre-req`, `ext-plugin-post-req`, `ext-plugin-post-resp`, and `inspect`. They require an external plugin runner or Lua/OpenResty runtime. The bounded `serverless-pre-function` and `serverless-post-function` compatibility implementations remain documented as not-required native/runtime parity.

## Status definitions

- **Supported (monitor):** registered, usable through the Go request/response pipeline, and covered by focused tests for the important paths.
- **Supported (README):** a registered Go implementation with the documented bounded behavior; the row is retained for an extra/native surface outside the 104-default count.
- **Deferred:** usable behavior exists, but completing parity needs a separate stream, transport, health, or runtime contract.
- **Not required (native/runtime):** the remaining behavior is native to NGINX, OpenResty, Lua, or an external plugin runtime and is intentionally outside normal Go-native parity.
- **Estimated parity:** a manually maintained feature-parity estimate, not Go test coverage or converted upstream-corpus coverage. The supported behavior and remaining jobs are the auditable source of truth.
- **Remaining jobs:** concrete compatibility boundaries or monitor-only follow-ups. No percentage-only implementation work is implied.

## Plugin status

The table below is the single source of truth for plugin status. The final column contains the per-plugin remaining jobs consolidated from the former execution plan and gap catalog. Supported-behavior and remaining-jobs cells use hyphen-prefixed items separated by `<br>` to keep every plugin on one physical table row.

| Category | Plugin | APISIX 3.17 scope | Registered | Estimated parity | Current status | Supported behavior | Remaining jobs |
|---|---|---|---:|---:|---|---|---|
| General | [`batch-requests`](https://apisix.apache.org/zh/docs/apisix/plugins/batch-requests/) | README-listed extra/native | yes | 85% | Supported (README) | - POST `/apisix/batch-requests` with `plugin_attr.batch-requests.uri` override<br>- global and per-item query/header merging with per-item host override<br>- HTTP version validation and default methods<br>- request body forwarding<br>- bounded timeout responses and response aggregation<br>- `X-Real-IP` subrequest injection<br>- metadata `max_body_size` / `max_pipeline_items`<br>- accepted `ssl_verify` input | - True HTTP pipelining is absent; the dispatcher runs each item as a separate in-process subrequest.<br>- Custom NGINX real-IP header configuration is not implemented; `X-Real-IP` is hardcoded.<br>- The accepted `ssl_verify` flag is not honored by the in-process dispatcher, which never performs TLS. |
| General | [`redirect`](https://apisix.apache.org/zh/docs/apisix/plugins/redirect/) | APISIX 3.17 default | yes | 95% | Supported (monitor) | - `uri` rewriting with `$var` / `${var}` / `\$` expansion<br>- `regex_uri` with compile-time validation<br>- `http_to_https` with forwarded HTTPS detection<br>- method-specific 301/308 responses<br>- query preservation including `append_query_string`<br>- robust host/port replacement<br>- `ret_code` and `encode_uri`<br>- `plugin_attr.redirect.https_port`<br>- random `apisix.ssl.listen` fallback | - No normal Go TODO; the schema omits the official `uri` pattern constraint and Lua lrucache, neither a Go-implementable surface.<br>- Monitor exact OpenResty URI/phase behavior only if a concrete mismatch appears. |
| General | [`echo`](https://apisix.apache.org/zh/docs/apisix/plugins/echo/) | APISIX 3.17 default | yes | 95% | Supported (monitor) | - `before_body`, `body`, `after_body` rewriting<br>- response `headers` with string or numeric values<br>- body-modification header cleanup<br>- official body/header schema boundaries | - No normal Go TODO; exact native phase timing (OpenResty body_filter chunk ordering) remains outside the Go middleware contract. |
| General | [`gzip`](https://apisix.apache.org/zh/docs/apisix/plugins/gzip/) | APISIX 3.17 default | yes | 80% | Supported (monitor) | - `types` and `types = "*"` wildcard matching<br>- `min_length` gating when the response provides `Content-Length`<br>- `comp_level`, `http_version`, and `vary`<br>- gzip and raw-deflate streaming writers selected by simple `Accept-Encoding` token detection | - `Accept-Encoding` q-values and preference order are not parsed, so values such as `gzip;q=0` can still select gzip.<br>- `deflate` emits raw RFC 1951 bytes rather than the zlib-wrapped representation expected for HTTP `Content-Encoding: deflate`.<br>- Unknown-length responses cannot apply `min_length` before streaming.<br>- NGINX-native `buffers` tuning is parsed but never applied; matching NGINX buffering internals is out of scope. |
| General | [`brotli`](https://apisix.apache.org/zh/docs/apisix/plugins/brotli/) | README-listed extra/native | yes | 78% | Supported (README) | - `Accept-Encoding` recognition for `br` / `*` with basic `q=0` disabling<br>- `types`, `min_length`, `comp_level`, `lgwin`, `http_version`, `vary`<br>- content-encoding skip and content-length removal<br>- strong ETag weakening | - Explicit `br` preferences do not consistently override earlier wildcard entries, and full q-value preference negotiation is not implemented.<br>- Go encoder `mode` / `lgblock` tuning is schema-accepted but inert; only Quality and LGWin reach the encoder.<br>- NGINX streaming internals (on-the-fly chunk compression) differ from the buffered transform writer. |
| General | [`real-ip`](https://apisix.apache.org/zh/docs/apisix/plugins/real-ip/) | APISIX 3.17 default | yes | 90% | Supported (monitor) | - `arg_*`, `http_*`, `cookie_*` source variables<br>- `http_x_forwarded_for` with recursive trusted-CIDR walk<br>- common built-in request variables<br>- bare IP and IP:port sources with valid port bounds<br>- `trusted_addresses` with config-time CIDR rejection<br>- `recursive`<br>- request-context `remote_addr` / `remote_port` updates | - Full NGINX variable catalog is not implemented; only the bounded set plus generic request-variable fallback resolve.<br>- Variable-cache flushing and APISIX-Base `set_real_ip` internals have no Go equivalent. |
| General | [`server-info`](https://apisix.apache.org/zh/docs/apisix/plugins/server-info/) | README-listed extra/native | yes | 75% | Supported (README) | - `/v1/server_info` response shape when enabled in `conf.plugins`<br>- configured `apisix.id` node IDs<br>- persisted `conf/apisix.uid` fallback IDs<br>- generated process fallback IDs<br>- periodic reporting to `data_plane/server_info/{id}`<br>- bounded `plugin_attr.server-info.report_ttl`<br>- lease keepalive<br>- shutdown stops renewal so the record expires | - Dynamic etcd-version lookup is not implemented; the reporter hardcodes the etcd version as unknown.<br>- Exact OpenResty shared-dict lifecycle is not replicated; the reporter is process-global. |
| General | [`error-page`](https://apisix.apache.org/zh/docs/apisix/plugins/error-page/) | README-listed extra/native | yes | 70% | Supported (README) | - official name, priority, empty route schema<br>- metadata-shaped `enable` and `error_404` / `error_500` / `error_502` / `error_503`<br>- custom body / content-type / content-length<br>- default APISIX-style HTML bodies<br>- upstream-sourced responses skipped via `$response_source` | - Exact header/body filter timing is not reproduced; official splits work across header_filter and body_filter.<br>- Metadata schema exposure requires a richer route/plugin interface; unknown metadata keys cannot be surfaced. |
| General | [`exit-transformer`](https://apisix.apache.org/zh/docs/apisix/plugins/exit-transformer/) | README-listed extra/native | yes | 55% | Supported (README) | - official name, priority, `functions` schema<br>- chained response capture<br>- documented status-remap Lua pattern<br>- normalized JSON error body / `X-Error-Code` header pattern<br>- upstream-sourced responses skipped via response provenance | - Configured Lua runs in a sandboxed gopher-lua runtime (base/table/string/math only; no os/io/debug/package.loadlib), so general control flow, table/header/body mutation and chained transformers execute.<br>- `core.response.exit()` callbacks and the full `ngx_lua`/OpenResty API surface remain deferred. |
| General | [`attach-consumer-label`](https://apisix.apache.org/zh/docs/apisix/plugins/attach-consumer-label/) | APISIX 3.17 default | yes | 80% | Supported (monitor) | - official name, priority, schema<br>- client-supplied header deletion before label mapping<br>- authenticated consumer label copy into request headers<br>- JSON serialization for numeric, boolean, and array label values | - No normal Go TODO; the only boundary is exact OpenResty phase fidelity (official runs in the rewrite phase with consumer context). |
| General | [`serverless-pre-function`](https://apisix.apache.org/zh/docs/apisix/plugins/serverless/) | APISIX 3.17 default | yes | 45% | Not required (native/runtime) | - official name, priority, schema<br>- Lua chunks returning functions with sequential execution<br>- `code` / `body` return short-circuiting<br>- `ngx.log`, `ngx.say`, `ngx.req.set_header`, `ngx.header`, `ngx.status`, `ngx.arg`<br>- `cjson` and selected `apisix.core` helpers | - Full OpenResty/Lua runtime is not required; `ngx.log` is a no-op, `ngx.ctx` is absent, and `apisix.core.ctx.register_var` is a stub.<br>- Shared dict/lrucache, custom variables, streaming chunks, and exact phase lifecycle are outside normal parity. |
| General | [`serverless-post-function`](https://apisix.apache.org/zh/docs/apisix/plugins/serverless/) | APISIX 3.17 default | yes | 45% | Not required (native/runtime) | - official name, priority, schema<br>- request-phase execution<br>- response capture for `header_filter` / `body_filter` / `log`<br>- response header/status/body mutation<br>- documented JSON body-filter rewrite pattern | - Full OpenResty/Lua runtime, shared dict/lrucache, custom variables, streaming chunks, and exact phase lifecycle are not required in normal parity; the Go emulation runs functions after the buffered response.<br>- Behavior is exercised indirectly as a fixture inside the error-page manifest. |
| General | [`azure-functions`](https://apisix.apache.org/zh/docs/apisix/plugins/azure-functions/) | APISIX 3.17 default | yes | 95% | Supported (monitor) | - official name, priority, schema, terminating handler<br>- method/query/body/header forwarding with wildcard `:ext` paths<br>- client/route/metadata authorization precedence<br>- encrypted route API keys and metadata master keys<br>- `X-Functions-Key` / `X-Functions-Clientid` injection<br>- status/body/header relaying<br>- HTTP/2 connection-header filtering<br>- `ssl_verify`, `keepalive`, `keepalive_timeout`, `keepalive_pool` | - No normal Go TODO; the shared function-upstream transport covers the full configurable surface. |
| General | [`openfunction`](https://apisix.apache.org/zh/docs/apisix/plugins/openfunction/) | APISIX 3.17 default | yes | 94% | Supported (monitor) | - official name, priority, schema, terminating handler<br>- method/query/body/header forwarding with wildcard `:ext` paths<br>- Basic authorization from encrypted `authorization.service_token`<br>- status/body/header relaying<br>- HTTP/2 connection-header filtering<br>- `ssl_verify`, `keepalive`, `keepalive_timeout`, `keepalive_pool` | - No normal Go TODO; the plugin shares the function-upstream transport with azure-functions and aws-lambda. |
| General | [`openwhisk`](https://apisix.apache.org/zh/docs/apisix/plugins/openwhisk/) | APISIX 3.17 default | yes | 93% | Supported (monitor) | - official name, priority, schema and name validation<br>- action endpoint construction with optional package<br>- POST body forwarding with Basic auth from encrypted `service_token`<br>- default `blocking` / `result` / `timeout` query parameters<br>- JSON result `statusCode` / scalar-or-list `headers` / body values<br>- invalid result fallback to 503<br>- `ssl_verify`, `keepalive`, `keepalive_timeout`, `keepalive_pool` | - No normal Go TODO; schema-name validation, endpoint building, and result mapping match the official plugin surface. |
| General | [`aws-lambda`](https://apisix.apache.org/zh/docs/apisix/plugins/aws-lambda/) | APISIX 3.17 default | yes | 95% | Supported (monitor) | - official name, priority, schema, terminating handler<br>- method/query/body/header forwarding with wildcard `:ext` paths<br>- encrypted API-key/IAM credentials<br>- `x-api-key` injection without overwriting client headers<br>- IAM SigV4 signing with APISIX-compatible canonicalization<br>- status/body/header relaying<br>- HTTP/2 connection-header filtering<br>- `ssl_verify`, `keepalive`, `keepalive_timeout`, `keepalive_pool` | - No normal Go TODO; SigV4 signing delegates to the shared AI auth signer with the documented canonicalization options. |
| General | [`ext-plugin-pre-req`](https://apisix.apache.org/zh/docs/apisix/plugins/ext-plugin-pre-req/) | APISIX 3.17 default | no | 0% | Not required (native/runtime) | - None; requires an external plugin runner. | - External plugin-runner protocol/runtime is not in Go-native scope; keep unregistered and documented. |
| General | [`ext-plugin-post-req`](https://apisix.apache.org/zh/docs/apisix/plugins/ext-plugin-post-req/) | APISIX 3.17 default | no | 0% | Not required (native/runtime) | - None; requires an external plugin runner. | - External plugin-runner protocol/runtime is not in Go-native scope; keep unregistered and documented. |
| General | [`ext-plugin-post-resp`](https://apisix.apache.org/zh/docs/apisix/plugins/ext-plugin-post-resp/) | APISIX 3.17 default | no | 0% | Not required (native/runtime) | - None; requires an external plugin runner. | - External plugin-runner protocol/runtime is not in Go-native scope; keep unregistered and documented. |
| General | [`inspect`](https://apisix.apache.org/zh/docs/apisix/plugins/inspect/) | APISIX 3.17 default | no | 0% | Not required (native/runtime) | - None; requires a Lua/OpenResty inspection runtime. | - Lua/OpenResty inspection runtime is not in Go-native scope; the official plugin instruments LuaJIT GC and phase hooks with no Go pipeline equivalent; keep unregistered and documented. |
| General | [`ocsp-stapling`](https://apisix.apache.org/zh/docs/apisix/plugins/ocsp-stapling/) | README-listed extra/native | no | 0% | Not supported (native) | - None; TLS-listener native behavior. | - NGINX/TLS stapling internals are not in Go-native scope; OCSP response caching, handshake stapling, and cert-chain verification belong to the TLS listener layer. |
| Transformation | [`response-rewrite`](https://apisix.apache.org/zh/docs/apisix/plugins/response-rewrite/) | APISIX 3.17 default | yes | 97% | Supported (monitor) | - `status_code` and validated plain/base64 `body`<br>- legacy and `add` / `set` / `remove` headers with string or numeric values<br>- response/request header variables<br>- nested `lua-resty-expr` logical groups<br>- comparison/regex/list/IP operators<br>- response body `filters`<br>- gzip/Brotli response decoding before filters | - Go RE2 replaces OpenResty PCRE (named captures, backtracking, gsub flag semantics differ); fix only on a reproduced mismatch.<br>- The response is buffered, so chunk-level body_filter semantics for filters are not replicated.<br>- `body_secret` is a Go opt-in extension versus official secret-ref detection in `body`; keep the boundary documented. |
| Transformation | [`proxy-rewrite`](https://apisix.apache.org/zh/docs/apisix/plugins/proxy-rewrite/) | APISIX 3.17 default | yes | 98% | Supported (monitor) | - `uri` rewriting with variable resolution<br>- `regex_uri` with first-match selection<br>- `use_real_request_uri_unsafe`<br>- `method` rewriting<br>- `host`<br>- request header `add` / `set` / `remove` and legacy header set<br>- bounded header-value variables plus `$1`-style regex captures<br>- rewrite applied through `ctx.FinalizeProxyRewrite` | - URI/method finalization currently waits until the before-proxy terminal, so lower-priority plugins observe the old request URI/method; 3.17 applies `ngx.req.set_uri` / `ngx.req.set_method` during rewrite and this must be aligned.<br>- Exact OpenResty URI normalization is not replicated; encoded paths and raw queries are covered.<br>- `scheme` is a Go-only extension; the official 3.17 schema has no `scheme` field — keep documented or drop for strict parity.<br>- `proxy_rewrite_regex_uri_captures` context variable has no Go registry consumer yet. |
| Transformation | [`grpc-transcode`](https://apisix.apache.org/zh/docs/apisix/plugins/grpc-transcode/) | APISIX 3.17 default | yes | 90% | Deferred (separate subsystem) | - base64 `.pb` FileDescriptorSet and plain `.proto` resources with content-keyed binding caching<br>- pure-Go imported-source and standard-import resolution<br>- string/integer `proto_id`<br>- GET scalar/repeated/dotted nested-message/scalar-map and bounded repeated-message query forms<br>- POST JSON mapping including repeated nested messages<br>- Lua-compatible `#` decimal/hex int64 request inputs<br>- gRPC request framing and integer-millisecond `grpc-timeout` values<br>- JSON response decoding with `enum_as_name` / `enum_as_value`<br>- all three int64 output modes including nested/list/map fields<br>- JSON-visible proto3 default-value modes<br>- gRPC-to-HTTP status mapping<br>- `grpc-status-details-bin` decoding with optional `status_detail_type`<br>- in-process gRPC-server integration coverage for unary success/errors | - The `deadline` schema accepts fractional numbers, which currently produce invalid values such as `1.5m`; restrict or normalize the field to the integer wire format.<br>- `use_default_metatable` and `enable_hooks` / `disable_hooks` are schema-accepted, but Lua metatable/hook semantics need a separate design.<br>- Streaming methods are rejected at binding load; the official response decode also assumes a single frame, so no official streaming gap exists today.<br>- Route binding is plugin-owned; a shared proto-resource/route contract would remove duplication with the etcd reload path. |
| Transformation | [`grpc-web`](https://apisix.apache.org/zh/docs/apisix/plugins/grpc-web/) | APISIX 3.17 default | yes | 75% | Deferred (separate subsystem) | - CORS preflight and existing-origin preservation<br>- route wildcard-to-gRPC path rewriting<br>- binary/text gRPC-Web request body translation<br>- upstream `application/grpc` content type<br>- response content-type restoration<br>- trailers-only status/message preservation<br>- `http.TrailerPrefix` `grpc-status` / `grpc-message` promotion<br>- binary response streaming with flush and request cancellation<br>- per-write Base64 encoding for text responses and basic trailer chunk encoding | - Text responses are encoded as independently padded Base64 blocks for each upstream write and trailer, rather than one continuous decodable gRPC-Web text stream; use a response-lifetime encoder.<br>- Invalid-method rejection differs from official: official returns 405 for non-POST while Go returns 400 (`pkg/plugin/grpc_web/plugin.go:85-88`); align on a reproduced mismatch.<br>- NGINX chunk-level body_filter semantics are approximated by the streaming writer; exact phase behavior needs a streaming contract.<br>- Trailers-only detection is inferred from buffered writer state rather than `sent_http_grpc_status`; monitor empty-body status edge cases. |
| Transformation | [`fault-injection`](https://apisix.apache.org/zh/docs/apisix/plugins/fault-injection/) | APISIX 3.17 default | yes | 97% | Supported (monitor) | - `abort` and `delay`<br>- omitted-vs-explicit `percentage`<br>- empty abort bodies<br>- string/numeric response headers<br>- response body/header variable resolution<br>- fractional delay seconds<br>- config-time `vars` validation<br>- OR across wrapped rules<br>- nested `AND` / `OR` / `!AND` / `!OR`<br>- comparison/regex/list/IP/negation operators | - The shared expr engine uses Go regexp and a bounded variable catalog, so exact PCRE semantics and the full NGINX variable list are not replicated.<br>- The fault runs at handler position, not the NGINX rewrite phase; interaction with rewrite-phase plugins is approximated.<br>- Random sampling gives statistical rather than deterministic percentage behavior. |
| Transformation | [`mocking`](https://apisix.apache.org/zh/docs/apisix/plugins/mocking/) | APISIX 3.17 default | yes | 97% | Supported (monitor) | - `response_example` preferred over `response_schema`<br>- schema object generation with `example` values<br>- JSON/plain-text/XML schema bodies<br>- response headers with bounded variable resolution<br>- `delay`, `response_status`, `content_type`, `with_mock_header` | - Official `gen_string` emits a random lowercase string while Go returns an empty string for example-less strings; monitor distribution.<br>- Official rejects unsupported content types while Go's schema has no such restriction and emits JSON for unknown types. |
| Transformation | [`degraphql`](https://apisix.apache.org/zh/docs/apisix/plugins/degraphql/) | APISIX 3.17 default | yes | 78% | Supported (monitor) | - bounded GraphQL syntax/structure validation<br>- multiple-operation `operation_name` enforcement<br>- GET/POST rewriting to `query`, `variables`, `operationName` | - Queries are parsed with the gqlparser AST; schema/type semantics and exact parser-error messages are not replicated because no GraphQL schema is loaded.<br>- Go adds `maxLength 1024` caps the official schema lacks; long-query corpus tests would diverge. |
| Transformation | [`body-transformer`](https://apisix.apache.org/zh/docs/apisix/plugins/body-transformer/) | APISIX 3.17 default | yes | 88% | Supported (monitor) | - request/response template substitution for `json`, `xml`, `yaml`, `encoded`, `args`, `plain`<br>- bounded `multipart` fields and file names<br>- nested values, array indexes and bracket paths<br>- repeated XML element indexes and `_attr.<name>` lookup<br>- namespace-local subtree serialization via `_escape_json()`<br>- repeated query/form values with numeric indexes<br>- bounded `..` concatenation, `+` addition, `string.gsub()`<br>- single/double-quoted string literals<br>- escaped `{{ expr }}` and raw `{* expr *}` output<br>- bounded `{% if %}` / `{% elseif %}` / `{% else %}` / `{% end %}` branches<br>- reserved-helper shadowing protection<br>- `_body` and `_ctx.var.*` request values<br>- `_escape_json()` / `_escape_xml()`<br>- APISIX-compatible Base64 template attempt/fallback<br>- oversized-request rejection via `client-control.max_body_size` | - Official templates are arbitrary Lua chunks, so loops, general function execution, and `local` statements remain deferred.<br>- Full XML namespace fidelity is not replicated; namespace rewrite differs on numeric keys and attribute namespaces.<br>- `_multipart` helper semantics are flattened to fields/file names.<br>- `input_format: yaml` is a Go extension; the official 3.17 enum is xml/json/encoded/args/plain/multipart. |
| Authentication | [`key-auth`](https://apisix.apache.org/zh/docs/apisix/plugins/key-auth/) | APISIX 3.17 default | yes | 75% | Supported (monitor) | - encrypted consumer fields<br>- header/query API key lookup<br>- APISIX-style missing/invalid key errors<br>- consumer attachment<br>- `hide_credentials` removal from headers or query strings<br>- `anonymous_consumer` fallback | - No normal Go TODO; every official field (header, query, realm, hide_credentials, anonymous_consumer) is wired end to end, and the WWW-Authenticate realm output is covered.<br>- Remaining margin is edge fidelity such as realm charset and digest-level equality, not missing features. |
| Authentication | [`jwt-auth`](https://apisix.apache.org/zh/docs/apisix/plugins/jwt-auth/) | APISIX 3.17 default | yes | 85% | Supported (monitor) | - `HS*`, `RS*`, `ES*`, `PS*`, `EdDSA` signature verification<br>- header/query/cookie token lookup<br>- `exp` / `nbf` claim verification<br>- `base64_secret` and PEM/PKIX/PKCS1 key parsing<br>- `hide_credentials`<br>- `store_in_ctx`<br>- `anonymous_consumer` fallback<br>- encrypted consumer fields through `apisix.data_encryption` | - No normal Go TODO; exact OpenResty session/cache lifecycle is not required — token verification is per-request stateless Go code.<br>- Remaining margin covers multi-algorithm key-parsing edge cases rather than any unimplemented field. |
| Authentication | [`jwe-decrypt`](https://apisix.apache.org/zh/docs/apisix/plugins/jwe-decrypt/) | APISIX 3.17 default | yes | 90% | Supported (monitor) | - compact direct AES-256-GCM JWE parsing<br>- `Bearer` token extraction<br>- `kid` consumer lookup<br>- required 32-byte plain/base64url consumer secrets<br>- `forward_header` plaintext forwarding<br>- `strict` mode rejection<br>- encrypted consumer fields | - No normal Go TODO; alternate JWE algorithms and AAD are not APISIX 3.17 configuration — the official schema only accepts `dir` / `A256GCM` with 32-byte secrets. |
| Authentication | [`basic-auth`](https://apisix.apache.org/zh/docs/apisix/plugins/basic-auth/) | APISIX 3.17 default | yes | 70% | Supported (monitor) | - Basic credential extraction<br>- APISIX-style credential whitespace normalization<br>- consumer attachment<br>- password validation<br>- missing/malformed authorization errors<br>- `hide_credentials`<br>- `anonymous_consumer` fallback<br>- encrypted consumer fields through `apisix.data_encryption` | - No normal Go TODO; all official fields are implemented and every failure branch attempts anonymous fallback.<br>- The conservative estimate reflects low-risk residual detail such as realm escaping. |
| Authentication | [`authz-keycloak`](https://apisix.apache.org/zh/docs/apisix/plugins/authz-keycloak/) | APISIX 3.17 default | yes | 85% | Supported (monitor) | - explicit `token_endpoint` or discovery-derived endpoint<br>- static `permissions` and lazy path resource lookup<br>- UMA decision requests<br>- `http_method_as_scope`<br>- `ENFORCING` access-denied behavior<br>- `access_denied_redirect_uri`<br>- `ssl_verify`, timeout, keepalive settings<br>- password-grant token generation URI<br>- process-shared discovery/service-account caching with `cache_ttl_seconds`<br>- refresh-token reuse and expiry leeway | - Lua `http_request_decorator` hooks and cross-process shared-dict fidelity remain deferred; Go caches are in-process maps guarded by mutexes.<br>- No 3.17 config field is affected. |
| Authentication | [`authz-casdoor`](https://apisix.apache.org/zh/docs/apisix/plugins/authz-casdoor/) | APISIX 3.17 default | yes | 84% | Supported (monitor) | - OAuth authorize redirect<br>- per-`client_id` session cookie<br>- callback state validation<br>- encrypted client secrets<br>- access token exchange against `/api/login/oauth/access_token`<br>- token-expiry enforcement<br>- authenticated session pass-through<br>- callback failure logging | - `randomState` falls back to a predictable time string when `crypto/rand` fails; session IDs and OAuth state must fail closed instead.<br>- Distributed/exact `resty.session` behavior remains deferred; sessions live in an in-process map with expiry rather than encrypted/shared session storage. |
| Authentication | [`dingtalk-auth`](https://apisix.apache.org/docs/apisix/plugins/dingtalk-auth/) | APISIX 3.17 default | yes | 65% | Supported (monitor) | - official name, priority, schema<br>- no-code redirect to `redirect_uri`<br>- code extraction from configurable header/query names<br>- APISIX-style invalid-code JSON errors<br>- access token POST with TTL caching<br>- userinfo POST<br>- signed `dingtalk_session` cookie<br>- `cookie_expires_in` and `secret_fallbacks` verification<br>- `ssl_verify`, timeout<br>- spoofed `X-Userinfo` clearing<br>- Base64 JSON `X-Userinfo` forwarding<br>- `$external_user` request-context propagation | - Exact encrypted `resty.session`, distributed state, logging, and worker-shared token-cache behavior remain deferred; sessions are signed-not-encrypted cookies with a per-instance token cache. |
| Authentication | [`feishu-auth`](https://apisix.apache.org/docs/apisix/plugins/feishu-auth/) | APISIX 3.17 default | yes | 65% | Supported (monitor) | - official name, priority, schema<br>- no-code redirect to `redirect_uri`<br>- code extraction from configurable header/query names<br>- OAuth token POST with `auth_redirect_uri`<br>- userinfo GET with Bearer token<br>- signed `feishu_session` cookie<br>- `cookie_expires_in` and `secret_fallbacks` verification<br>- `ssl_verify`, timeout<br>- spoofed `X-Userinfo` clearing<br>- Base64 JSON `X-Userinfo` forwarding<br>- `$external_user` request-context propagation | - Exact encrypted `resty.session`, distributed state, logging, and worker/session behavior remain deferred; sessions are signed-not-encrypted cookies with in-process validation. |
| Authentication | [`saml-auth`](https://apisix.apache.org/docs/apisix/plugins/saml-auth/) | APISIX 3.17 default | yes | 85% | Supported (monitor) | - HTTP-Redirect and HTTP-POST authentication requests<br>- SP-signed SAML requests<br>- ACS `SAMLResponse` parsing, signature and condition validation through `github.com/crewjam/saml`<br>- signed local SAML session cookies<br>- `secret_fallbacks` verification<br>- SP-initiated logout redirect<br>- logout callback cleanup<br>- `X-Userinfo` forwarding<br>- local `$external_user` attachment<br>- standalone coverage of both bindings, correlated callbacks, invalid state/correlation/signature paths, wrong ACS, and two-SP isolation | - Exact `lua-resty-saml` session/runtime behavior remains deferred; SAML message semantics themselves are implemented, so this is a runtime-fidelity boundary, not a missing protocol feature. |
| Authentication | [`wolf-rbac`](https://apisix.apache.org/zh/docs/apisix/plugins/wolf-rbac/) | APISIX 3.17 default | yes | 75% | Supported (monitor) | - `V1#appid#wolf_token` parsing<br>- token extraction from query/header/cookie<br>- consumer lookup by `appid`<br>- Wolf `/wolf/rbac/access_check`<br>- configured TLS verification<br>- transient 5xx retry/backoff<br>- user info header injection<br>- consumer attachment<br>- public login/change-password/user-info APIs at `/apisix/plugin/wolf-rbac/*` | - Cross-process OpenResty consumer-cache fidelity remains deferred; Go queries the store on every request while APISIX caches consumer lookups in worker-shared dicts. |
| Authentication | [`openid-connect`](https://apisix.apache.org/zh/docs/apisix/plugins/openid-connect/) | APISIX 3.17 default | yes | 98% | Supported (monitor) | - Bearer token extraction from `Authorization` and `X-Access-Token`<br>- discovery fallback for `introspection_endpoint`<br>- token introspection<br>- `client_secret_basic` / `client_secret_post` / `private_key_jwt` / `client_secret_jwt` client authentication<br>- `public_key` and `use_jwks` RSA JWT verification<br>- `token_signing_alg_values_expected`, `required_scopes`<br>- `claim_validator.audience` required/client-id matching<br>- bearer and session-flow `claim_schema` validation<br>- `realm`, `unauth_action = pass`, output header clearing<br>- `X-Access-Token`, `X-Userinfo`, `ssl_verify`, `timeout`, `introspection_addon_headers`<br>- `proxy_opts` HTTP/HTTPS proxy selection with Basic credentials and `no_proxy` bypasses<br>- cookie and Redis sessions with encrypted AES-GCM state<br>- opaque encrypted Redis cookie IDs<br>- Redis TLS/auth/database/prefix/timeouts<br>- state validation and PKCE S256<br>- configured/automatic redirect URIs, `authorization_params`, `force_reauthorize`, token exchange<br>- `renew_access_token_on_expiry`, `access_token_expires_in`, `access_token_expires_leeway`<br>- silent `refresh_session_interval` reauthentication<br>- optional userinfo forwarding and end-session logout<br>- `revoke_tokens_on_logout` refresh/access-token revocation | - Static `public_key` verification reparses PEM/x509 material on every bearer request; parse it once after secret resolution in `PostInit`.<br>- Exact `lua-resty-session` runtime behavior remains deferred; session semantics are implemented in Go, but byte-level resty.session storage/rotation behavior is not replicated.<br>- No 3.17 config field is missing. |
| Authentication | [`cas-auth`](https://apisix.apache.org/zh/docs/apisix/plugins/cas-auth/) | APISIX 3.17 default | yes | 85% | Supported (monitor) | - CAS login redirect<br>- absolute/relative `cas_callback_uri`<br>- ticket `serviceValidate`<br>- HMAC-signed initiation cookie<br>- per-config session cookie<br>- local session refresh<br>- logout redirect<br>- IdP single-logout XML `SessionIndex` session deletion | - OpenResty shared-dict clustering remains deferred; sessions are an in-process expirable LRU.<br>- User metadata forwarding is not APISIX 3.17 behavior — official cas-auth only stores/validates the user string. |
| Authentication | [`hmac-auth`](https://apisix.apache.org/zh/docs/apisix/plugins/hmac-auth/) | APISIX 3.17 default | yes | 82% | Supported (monitor) | - `hmac-sha1`, `hmac-sha256`, `hmac-sha512` verification<br>- `signed_headers`<br>- `clock_skew`<br>- request body digest validation<br>- APISIX-style Signature header parsing<br>- `hide_credentials`<br>- `anonymous_consumer` fallback | - No normal Go TODO; all official fields (including validate_request_body and max_req_body_size) are implemented and manifest-covered. |
| Authentication | [`authz-casbin`](https://apisix.apache.org/zh/docs/apisix/plugins/authz-casbin/) | APISIX 3.17 default | yes | 85% | Supported (monitor) | - Casbin `model` / `policy` text config<br>- `model_path` / `policy_path` file config<br>- plugin-metadata `model` / `policy` fallback with reload on update<br>- configured username header<br>- anonymous fallback<br>- 403 / 503 error responses | - No normal Go TODO; both config paths, metadata fallback, and reload-on-update are implemented and covered by the metadata-policy-lifecycle scenario. |
| Authentication | [`ldap-auth`](https://apisix.apache.org/zh/docs/apisix/plugins/ldap-auth/) | APISIX 3.17 default | yes | 75% | Supported (monitor) | - HTTP Basic credential extraction<br>- LDAP bind using configured `base_dn`<br>- host/URL `ldap_uri`<br>- `uid` attribute for user DN construction<br>- direct-LDAPS `use_tls`<br>- `tls_verify`<br>- consumer match by `ldap-auth.user_dn`<br>- consumer context attachment | - Do not add LDAP search filters, StartTLS, or anonymous-consumer behavior absent from the official 3.17 schema (only base_dn, ldap_uri, use_tls, tls_verify, uid, realm). |
| Authentication | [`opa`](https://apisix.apache.org/zh/docs/apisix/plugins/opa/) | APISIX 3.17 default | yes | 78% | Supported (monitor) | - OPA HTTP decision calls<br>- custom deny status/body/headers<br>- `send_headers_upstream`<br>- `with_consumer`<br>- `with_route` / `with_service` full resource documents<br>- ID/name/matched-URI fallback for direct plugin use | - No normal Go TODO; preserve the route/service resource-context contract (`SetResourceContext`). |
| Authentication | [`forward-auth`](https://apisix.apache.org/zh/docs/apisix/plugins/forward-auth/) | APISIX 3.17 default | yes | 90% | Supported (monitor) | - `GET` / `POST` with body and transport metadata forwarding<br>- `request_headers`<br>- `extra_headers` with variable resolution<br>- `upstream_headers` and `client_headers`<br>- `ssl_verify`, `keepalive`, `keepalive_timeout`, `keepalive_pool`<br>- `allow_degradation`, `status_on_error`, `timeout` | - No normal Go TODO; the official 3.17 schema requires string `extra_headers` values, and its numeric runtime fallback is defensive, not a configurable feature. |
| Authentication | [`multi-auth`](https://apisix.apache.org/zh/docs/apisix/plugins/multi-auth/) | APISIX 3.17 default | yes | 85% | Supported (monitor) | - ordered fallback across every 3.17 `type = auth` plugin: `basic-auth`, `key-auth`, `jwt-auth`, `hmac-auth`, `ldap-auth`, `jwe-decrypt`, `wolf-rbac`<br>- request passes when any configured auth plugin succeeds<br>- generic final 401 `Authorization Failed` denial<br>- per-plugin schema validation | - No normal Go TODO; preserve generic final-denial semantics and failure diagnostics through probe recording. |
| Security | [`cors`](https://apisix.apache.org/zh/docs/apisix/plugins/cors/) | APISIX 3.17 default | yes | 87% | Supported (monitor) | - `allow_origins` with defaults<br>- `allow_origins = "**"` request-origin echo<br>- `allow_origins_by_regex`<br>- `allow_origins_by_metadata`<br>- `timing_allow_origins` and `timing_allow_origins_by_regex`<br>- method wildcards<br>- `allow_headers = "**"` request-header reflection<br>- APISIX-style 200 preflight responses<br>- explicit exposed headers with no custom expose header by default<br>- `max_age`<br>- wildcard/credential cross-field validation | - Only exact middleware-specific wildcard response-header semantics remain to monitor; behavior rides on the rs/cors library plus APISIX-specific header code, so changes are triggered only by a concrete corpus mismatch. |
| Security | [`acl`](https://apisix.apache.org/zh/docs/apisix/plugins/acl/) | APISIX 3.17 default | yes | 78% | Supported (monitor) | - authenticated consumer `labels`<br>- string, JSON/segmented-text, numeric, boolean, and array label values<br>- `$external_user` label extraction with bounded dotted fields<br>- `$..field[.suffix]` recursive lookup<br>- `json` / `segmented_text` / `table` parsers<br>- allow/deny maps with `rejected_code` / `rejected_msg` | - Full OpenResty JSONPath/parser behavior remains deferred; field paths reject bracket/array syntax beyond the enumerated forms. |
| Security | [`uri-blocker`](https://apisix.apache.org/zh/docs/apisix/plugins/uri-blocker/) | APISIX 3.17 default | yes | 95% | Supported (monitor) | - `block_rules`<br>- safe invalid-regex rejection<br>- normalized-path matching with original query arguments<br>- concatenated-rule diagnostics<br>- `rejected_code`, `rejected_msg`, `case_insensitive`<br>- APISIX-style empty default rejection bodies<br>- `error_msg` JSON custom rejections | - Monitor PCRE/JIT exactness only after a concrete mismatch; Go's RE2 cannot express lookaround/backref patterns that Lua PCRE accepts. |
| Security | [`ip-restriction`](https://apisix.apache.org/zh/docs/apisix/plugins/ip-restriction/) | APISIX 3.17 default | yes | 95% | Supported (monitor) | - `whitelist` / `blacklist`<br>- CIDR/IP matching<br>- custom messages<br>- `response_code` 403/404 selection<br>- fail-fast IP/CIDR validation<br>- `remote_addr` context overrides<br>- APISIX-style JSON rejection bodies | - Shared OpenResty LRU matcher caching remains deferred; Go keeps a process-local per-request matcher with no cross-worker shared cache. |
| Security | [`ua-restriction`](https://apisix.apache.org/zh/docs/apisix/plugins/ua-restriction/) | APISIX 3.17 default | yes | 95% | Supported (monitor) | - mutually exclusive `allowlist` or `denylist` schema branches<br>- allow-before-deny matching<br>- `bypass_missing`<br>- trimmed User-Agent matching<br>- multi-value User-Agent enumeration<br>- APISIX-style JSON rejection bodies | - Exact OpenResty multi-value User-Agent and regex/cache fidelity remain deferred; matching is O(n) per request with no lrucache, and invalid patterns are skipped with a warning rather than rejected. |
| Security | [`referer-restriction`](https://apisix.apache.org/zh/docs/apisix/plugins/referer-restriction/) | APISIX 3.17 default | yes | 98% | Supported (monitor) | - `whitelist` / `blacklist`<br>- `bypass_missing` including malformed bare-host Referer values<br>- custom rejection messages<br>- APISIX-style JSON rejection bodies<br>- leading-`*` suffix host matching<br>- `host_def` character/wildcard schema validation | - Exact OpenResty host parsing/cache lifecycle remains deferred; goreferrer plus O(n) suffix scans replace the Lua regex host extraction and lrucache, so revisit only on a concrete mismatch. |
| Security | [`consumer-restriction`](https://apisix.apache.org/zh/docs/apisix/plugins/consumer-restriction/) | APISIX 3.17 default | yes | 85% | Supported (monitor) | - `consumer_name`, `service_id`, `route_id`, `consumer_group_id`<br>- blacklist and whitelist matching<br>- `allowed_by_methods` with the official 10-method enum<br>- custom rejection status/message<br>- APISIX-style rejection bodies | - Exact OpenResty consumer-context lifecycle remains deferred; identity comes from Go request-context variables rather than ngx consumer state, and per-request matching is a linear scan. |
| Security | [`csrf`](https://apisix.apache.org/zh/docs/apisix/plugins/csrf/) | APISIX 3.17 default | yes | 80% | Supported (monitor) | - token cookie/header presence and equality checks<br>- safe-method bypass<br>- expiry and HMAC-SHA256 signature validation<br>- `expires = 0` no-expiry validation<br>- configurable `key` / `expires` / `name`<br>- strict `key` resolution through `apisix.data_encryption`<br>- APISIX-style JSON error bodies | - Exact Lua random-number formatting remains deferred unless a user-visible mismatch appears; Go uses float `%v` formatting and `math/rand` instead of Lua's random generation. |
| Security | [`public-api`](https://apisix.apache.org/zh/docs/apisix/plugins/public-api/) | APISIX 3.17 default | yes | 75% | Supported (monitor) | - method+URI registry with `Register` / `Lookup`<br>- optional `uri` override<br>- `node-status` at `/apisix/status`<br>- `server-info` at `/v1/server_info`<br>- `batch-requests`<br>- Prometheus metrics endpoint registration | - No normal Go TODO for the runtime-owned batch/node-status/server-info/Prometheus registry; arbitrary internal API discovery remains out of scope — new internal APIs require an owner to register them. |
| Security | [`GM`](https://apisix.apache.org/zh/docs/apisix/plugins/GM/) | README-listed extra/native | yes | 25% | Supported (README) | - official name `gm` and priority matching pinned gm.lua<br>- empty route schema<br>- no-op HTTP handler<br>- SSL `gm` marker validation helper requiring encryption cert/key plus exactly one sign cert/key pair (unit-tested) | - The production SSL loading path does not yet invoke the `gm` marker validation; wire it in before claiming enforcement.<br>- Real Tongsuo/NTLS/SM2/SM3/SM4 serving and dynamic dual-certificate installation remain out of scope. |
| Security | [`chaitin-waf`](https://apisix.apache.org/zh/docs/apisix/plugins/chaitin-waf/) | APISIX 3.17 default | yes | 79% | Supported (monitor) | - `mode` off/monitor/block<br>- bounded nested `match.vars` expression operators<br>- official metadata schema<br>- metadata/config `nodes` with precedence<br>- round-robin selection with five-minute failure quarantine<br>- timeout/body/keepalive defaults<br>- monitor/block/off decision behavior<br>- request body replay before proxying<br>- official WAF response headers<br>- block response body with `event_id` | - The current replay restores only the `req_body_size`-truncated inspection copy; the WAF must receive the bounded copy while the upstream receives the complete original body.<br>- Native SafeLine integration and exact runtime protocol fidelity remain deferred; requests are forwarded via a plain Go HTTP client rather than the SafeLine Lua library. |
| Security | [`data-mask`](https://apisix.apache.org/zh/docs/apisix/plugins/data-mask/) | APISIX 3.17 default | yes | 80% | Supported (monitor) | - query/header/urlencoded-body masking<br>- bounded `max_req_post_args` prefix parsing<br>- APISIX conditional rule-schema validation<br>- bounded JSONPath body masking for dot paths, root-array selectors, quoted bracket fields, recursive descent, `[*]`, and numeric indexes<br>- `remove` / `replace` / `regex` actions<br>- `max_body_size`<br>- official name/schema/priority<br>- masked `$request_uri` / `$request_line` visible to downstream loggers | - Broader JSONPath dialect, temporary-file/log-phase timing, and exact OpenResty request-context semantics remain deferred; the parser rejects unions, slices, and unbracketed expressions. |
| Security | [`oas-validator`](https://apisix.apache.org/docs/apisix/plugins/oas-validator/) | APISIX 3.17 default | yes | 99% | Supported (monitor) | - inline JSON `spec`<br>- `spec_url` fetch with custom headers<br>- `timeout` and `ssl_verify`<br>- method/path matching with specificity ranking and server prefixes (kin-openapi router)<br>- required path/query/header/cookie parameters and OAS parameter styles (form, deepObject, spaceDelimited, pipeDelimited, simple, matrix, label)<br>- JSON/XML/YAML/form/multipart/text/octet-stream request bodies<br>- local and external component refs<br>- skip flags<br>- `reject_if_not_match`, `verbose_errors`<br>- configurable rejection status<br>- `spec_url_ttl` metadata refresh | - Validation is kin-openapi-backed; parameter style/location mismatches and malformed specs are rejected at spec load instead of per request.<br>- Delimited (space/pipe) object query parameters, exploded cookie objects/arrays, non-JSON content parameters, unprefixed exploded form objects and repeated deepObject/delimited values are rejected (kin-openapi does not implement them); repeated non-exploded form values are accepted.<br>- Response validation is a separate extension with no response-phase code. |
| Traffic | [`limit-req`](https://apisix.apache.org/zh/docs/apisix/plugins/limit-req/) | APISIX 3.17 default | yes | 95% | Supported (monitor) | - local, Redis, and Redis Cluster token-bucket rate limiting<br>- Redis/Cluster TLS, timeout, pool config<br>- route-scoped keys and `key_type = var` / `var_combination`<br>- HTTP header variables<br>- `rejected_code` with APISIX-style empty/custom bodies<br>- `nodelay`<br>- `allow_degradation` | - The process-shared `consumerBucketStores` registry has bounded inner buckets but no owner release for outer configuration keys; add reference-counted acquire/release at plugin lifecycle boundaries.<br>- Local buckets are per-plugin-instance maps, so exact cross-request shared-dict timing parity with lua-resty-limit-traffic remains native. |
| Traffic | [`limit-conn`](https://apisix.apache.org/zh/docs/apisix/plugins/limit-conn/) | APISIX 3.17 default | yes | 96% | Supported (monitor) | - local, Redis, and Redis Cluster concurrent-connection limiting<br>- Redis/Cluster TLS, timeout, pool, `key_ttl` config<br>- route-scoped admission/release keys<br>- adaptive unit-delay feedback<br>- top-level and rule-level string variable `conn` / `burst`<br>- `rules`<br>- `key_type = var`, `var_combination`, header variables<br>- `rejected_code` and empty/custom bodies<br>- `only_use_default_delay`, `allow_degradation` | - No normal Go TODO; per-route-local connection maps and in-process delay state cannot reproduce NGINX shared-dict counting and log-phase timing exactly. |
| Traffic | [`limit-count`](https://apisix.apache.org/zh/docs/apisix/plugins/limit-count/) | APISIX 3.17 default | yes | 97% | Supported (monitor) | - local, Redis, and Redis Cluster fixed-window quotas<br>- root-level and legacy nested Redis configs<br>- TLS/pool settings<br>- top-level and rule-level string variable `count` / `time_window`<br>- `rules` with per-rule `header_prefix`<br>- shared `group` quotas with mismatch validation<br>- route-scoped keys and `key_type` var/constant/var_combination<br>- quota headers and plugin-metadata custom names<br>- `rejected_code` and empty/custom bodies<br>- `allow_degradation` | - The process-shared group registry has bounded inner counters but no owner release for outer group keys; add reference-counted acquire/release at plugin lifecycle boundaries.<br>- `resty.limit.count`'s Redis counter layout and expiry semantics are approximated through the ulule store, so exact byte-level parity remains native. |
| Traffic | [`graphql-limit-count`](https://apisix.apache.org/docs/apisix/plugins/graphql-limit-count/) | APISIX 3.17 default | yes | 97% | Supported (monitor) | - official name, priority, schema<br>- POST `application/json` / `application/graphql` bounded by `graphql.max_size`<br>- selection-depth cost counting with alias/argument/directive syntax<br>- undefined-fragment rejection; cyclic fragments bounded via a visited stack<br>- static group-config mismatch rejection<br>- local, standalone Redis, and Redis Cluster fixed-window quotas<br>- official Redis/TLS/pool fields<br>- top-level and rule-level string variable `count` / `time_window`, `rules`, per-rule `header_prefix`<br>- shared `group` quotas and route-scoped Redis keys<br>- limit-count metadata header names<br>- `allow_degradation`, `rejected_code`, `rejected_msg`, `key`, `key_type`<br>- fragment/inline-fragment/chained-fragment depth expansion | - The process-shared group registry has bounded inner counters but no owner release for outer group keys; add reference-counted acquire/release at plugin lifecycle boundaries.<br>- Depth is computed over a gqlparser AST and mirrors official `graphql.lua` depth semantics; `resty.limit.count` counter exactness remains deferred.<br>- Cyclic fragments are bounded, not rejected — matching the official plugin. |
| Traffic | [`proxy-cache`](https://apisix.apache.org/zh/docs/apisix/plugins/proxy-cache/) | APISIX 3.17 default | yes | 98% | Supported (monitor) | - in-memory and configured absolute disk zones with atomic versioned envelopes<br>- cross-plugin-instance reload, persisted `Vary` indexes, `PURGE`<br>- observed-expired cleanup and lifecycle-owned background expiry sweeps<br>- configured memory-zone sharing across instances and with `graphql-proxy-cache`<br>- static zone-registry preflight and validated dynamic zone snapshots<br>- duplicate/empty name and bounded `cache_levels` validation<br>- cache strategy/zone storage matching<br>- `cache_key`, `cache_method`, `cache_http_status`, `cache_ttl`, `cache_bypass`, `no_cache`, `hide_cache_headers`, `consumer_isolation`<br>- memory-only `cache_control` request/TTL behavior<br>- memory-opt-in `cache_set_cookie`; disk never stores `Set-Cookie`<br>- `Age` on hits and upstream `private` / `no-store` / `no-cache` non-storage<br>- upstream `s-maxage` / `max-age` / `Expires` TTL derivation<br>- `Apisix-Cache-Status`<br>- write-time `disk_size` eviction | - A HEAD miss currently stores an empty-body entry under the same key as GET; HEAD may reuse a prior GET entry but must not create or overwrite the shared cache entry.<br>- Full NGINX cache-manager process orchestration and cross-worker cache-key/index sharing cannot be replicated in a single Go process; disk and memory envelope semantics are implemented, but native cache-manager fidelity remains deferred. |
| Traffic | [`graphql-proxy-cache`](https://apisix.apache.org/docs/apisix/plugins/graphql-proxy-cache/) | APISIX 3.17 default | yes | 95% | Supported (monitor) | - official name, priority, schema<br>- GET/POST validation bounded by `graphql.max_size`<br>- grammar parsing for operations, variables, types, args, directives, fragments, aliases, values, strings, numbers<br>- JSON and `application/graphql` bodies<br>- mutation bypass with `Apisix-Cache-Status: BYPASS`<br>- route/service/host/consumer-isolated MD5 cache keys and `APISIX-Cache-Key`<br>- configured memory-zone sharing and disk-zone persistence/expiry cleanup<br>- disk-zone `s-maxage` / `max-age` / `Expires` TTL derivation with `cache_ttl` fallback<br>- unconditional `private` / `no-store` / `no-cache` non-storage<br>- `consumer_isolation` and memory-only `cache_set_cookie` opt-in<br>- route-aware public `PURGE` endpoint | - The parser validates GraphQL grammar only, not schema/type semantics.<br>- Lookup/store phases are per-plugin rather than NGINX cache-manager driven; exact OpenResty phase and cache-manager behavior remain deferred. |
| Traffic | [`request-validation`](https://apisix.apache.org/zh/docs/apisix/plugins/request-validation/) | APISIX 3.17 default | yes | 89% | Supported (monitor) | - JSON and `application/x-www-form-urlencoded` `body_schema`<br>- JSON body normalization before proxying<br>- nested-schema validation at configuration time<br>- repeated-header array validation and `header_schema`<br>- `rejected_code` / `rejected_msg` for schema and decode failures | - JSON normalization currently decodes untyped numbers as `float64`, so integers above 2^53 can change before reaching upstream; use `Decoder.UseNumber` while retaining normalize-after-validation semantics.<br>- OpenResty-style header normalization is approximated, and `$ENV://` secret references inside schemas are not implemented; both remain deferred. |
| Traffic | [`proxy-mirror`](https://apisix.apache.org/zh/docs/apisix/plugins/proxy-mirror/) | APISIX 3.17 default | yes | 73% | Supported (monitor) | - HTTP mirror `host`, `path`, `path_concat_mode`, `sample_ratio`<br>- APISIX-style `host` / `path` schema validation<br>- bounded unary gRPC request mirroring over HTTP/2<br>- async mirror send with bounded timeout | - Mirroring resolves `host` directly without APISIX's DNS resolver; streaming/body-filter phase fidelity for long-lived responses is not reproduced; both remain deferred. |
| Traffic | [`kafka-proxy`](https://apisix.apache.org/docs/apisix/plugins/kafka-proxy/) | APISIX 3.17 default | yes | 75% | Supported (monitor) | - official name, priority, schema<br>- strict `sasl.password` secret resolution<br>- request-context propagation to the stream consumer<br>- APISIX PubSub protobuf WebSocket owner (`cmd_kafka_list_offset`, `cmd_kafka_fetch`) with sequence preservation<br>- offset/timestamp/key/value conversion through a bounded `kafka-go` consumer<br>- PLAIN SASL credentials<br>- upstream TLS `verify` and inline client certificate/key<br>- `client_cert_id` lookup through the local SSL resource bucket | - External-broker smoke testing is optional and unimplemented.<br>- Mechanisms beyond PLAIN and REST semantics are outside the APISIX 3.17 schema. |
| Traffic | [`dubbo-proxy`](https://apisix.apache.org/docs/apisix/plugins/dubbo-proxy/) | Registered non-default | yes | 82% | Deferred (separate subsystem) | - official name, priority, schema<br>- required `service_name` / `service_version`<br>- URI-derived method fallback<br>- Hessian2 `Map<String,Object>` HTTP-context request encoding<br>- route-upstream TCP terminal integration with retries<br>- provider response map conversion to HTTP status/headers/body<br>- request cancellation via context-driven socket close<br>- `upstream_multiplex_count` as a cancellation-aware per-target in-flight bound | - Active probe heartbeats are not sent, and every invocation opens a fresh TCP connection with a monotonic request ID, so persistent shared-connection multiplexing and response-ID matching are genuinely absent.<br>- Exact OpenResty/Tengine connection lifecycle remains deferred. |
| Traffic | [`http-dubbo`](https://apisix.apache.org/docs/apisix/plugins/http-dubbo/) | APISIX 3.17 default | yes | 75% | Supported (monitor) | - official name, priority, schema<br>- route-upstream TCP dialing with bounded connect/send/read timeouts<br>- Dubbo 2.x fastjson request frame construction<br>- `service_name`, `service_version`, `method`, `params_type_desc`<br>- `serialized` and `serialization_header_key`<br>- JSON-array generic invocation serialization with APISIX-compatible escaping<br>- pre-serialized body passthrough<br>- response-size/malformed-frame rejection and transport-error mapping<br>- Dubbo header/status parsing with HTTP 200 body mapping<br>- bounded connect-only retries from upstream `retries` | - Active probes are not implemented, and fastjson escaping edge cases cannot be verified without a reproduced mismatch against a real Dubbo provider; both are deferred until a concrete reproduction appears. |
| Traffic | [`api-breaker`](https://apisix.apache.org/zh/docs/apisix/plugins/api-breaker/) | APISIX 3.17 default | yes | 95% | Supported (monitor) | - `break_response_code`, `break_response_body`<br>- `break_response_headers` with bounded variable resolution<br>- `max_breaker_sec` with exponential backoff<br>- `unhealthy.http_statuses` / `unhealthy.failures`<br>- `healthy.http_statuses` / `healthy.successes` | - Breaker counters are route-local instead of host+URI shared across routes, and log-phase recovery timing is not reproduced; shared-dict and log-phase exactness remain native, with user-visible parity deferred until a concrete need. |
| Traffic | [`traffic-split`](https://apisix.apache.org/zh/docs/apisix/plugins/traffic-split/) | APISIX 3.17 default | yes | 96% | Supported (monitor) | - ordered `match.vars` with config-time expression compile<br>- pre-match resolution of every referenced `upstream_id`<br>- weighted inline/upstream-ID targets and route fallback entries<br>- explicit zero weights including referenced upstream resources<br>- numeric/string `upstream_id`, `pass_host` / `upstream_host`<br>- bracketed IPv6 node targets<br>- deterministic `chash` selection for vars/header/cookie/consumer<br>- `vars_combinations` templates with `$variable` concatenation and bounded `??` defaults | - The current request-context timeout uses the minimum of connect/send/read as one whole-request deadline and can cancel a valid long read; remove that unsafe approximation until a phase-specific transport contract exists.<br>- Active probes, exact phase-specific deadlines, full weighted-balancer fidelity, and the complete APISIX expression language are not implemented. |
| Traffic | [`traffic-label`](https://apisix.apache.org/zh/docs/apisix/plugins/traffic-label/) | APISIX 3.17 default | yes | 96% | Supported (monitor) | - schema-validated first-match rules<br>- match-all rules<br>- string/numeric `set_headers` with variable resolution<br>- weighted actions via per-weight round-robin sequences<br>- nested `AND` / `OR` / `!AND` / `!OR`<br>- comparison/regex/list/IP/negation operators | - Action selection uses a sequential cursor instead of cached random round-robin, the NGINX variable catalog is bounded, and access-phase timing differs from OpenResty; all three remain deferred. |
| Traffic | [`request-id`](https://apisix.apache.org/zh/docs/apisix/plugins/request-id/) | APISIX 3.17 default | yes | 90% | Supported (monitor) | - custom header names via `header_name`<br>- response header opt-out via `include_in_response`<br>- incoming request ID preservation<br>- `uuid`, `uuidv7`, `nanoid`, `ksuid`, `range_id` algorithms | - No normal Go TODO; all five algorithms and context propagation are complete, with only minor ID byte-layout edge parity remaining. |
| Traffic | [`proxy-control`](https://apisix.apache.org/zh/docs/apisix/plugins/proxy-control/) | APISIX 3.17 default | yes | 75% | Supported (monitor) | - route/global `request_buffering` flag propagation<br>- eager Go proxy request-body buffering before upstream forwarding | - APISIX-Runtime/NGINX dynamic and disk-backed buffering controls remain unsupported; only the single boolean flag at the request-body boundary is implemented. |
| Traffic | [`proxy-buffering`](https://apisix.apache.org/zh/docs/apisix/plugins/proxy-buffering/) | APISIX 3.17 default | yes | 75% | Supported (monitor) | - route/global `disable_proxy_buffering` flag propagation<br>- immediate response flushing via the flushing response writer<br>- streaming handler selection when buffering is disabled | - NGINX buffering internals (buffer sizes, partial-buffer flushing, disk-backed spill) are not reproduced; only the immediate-flush toggle is implemented. |
| Traffic | [`client-control`](https://apisix.apache.org/zh/docs/apisix/plugins/client-control/) | APISIX 3.17 default | yes | 90% | Supported (monitor) | - `max_body_size` enforcement at the route request-body boundary<br>- 413 rejection for oversized bodies (Content-Length and chunked)<br>- body restore to the request stream after a successful eager read | - Non-size request-body read errors are currently ignored and can forward a partial body; return an explicit error and add a failing-reader regression test.<br>- The official plugin delegates to APISIX-Runtime's `set_client_max_body_size`; the Go equivalent remains an eager route-level read rather than native streaming enforcement. |
| Traffic | [`workflow`](https://apisix.apache.org/zh/docs/apisix/plugins/workflow/) | APISIX 3.17 default | yes | 85% | Supported (monitor) | - official action-array config shape (`["name", config]`)<br>- first matching `case` with config-time expression compile<br>- bounded APISIX expression operators and request variables<br>- validated `return` actions with configured status code<br>- delegated `limit-req` / `limit-count` / `limit-conn` actions | - Only the four official action types are compiled; unknown actions fail at config time and limit-count `group` is rejected, so new APISIX actions or a concrete expression mismatch are the only triggers for further work. |
| Observability / Tracers | [`zipkin`](https://apisix.apache.org/zh/docs/apisix/plugins/zipkin/) | APISIX 3.17 default | yes | 82% | Deferred (native/runtime) | - B3 single-header and multi-header extraction/injection<br>- parent/child span identity with random IDs<br>- probabilistic `sample_ratio`<br>- `endpoint`, `service_name`, `server_addr`<br>- local/remote endpoints with IPv4/IPv6 and port<br>- status/error tags<br>- Zipkin v2 span reporting | - Cached tracer/shared-buffer behavior is absent; official caches a per-server opentracing tracer and tracks per-phase end times, while Go creates a fresh span per request.<br>- OpenResty batching is absent; official reports through the batch processor while Go POSTs each span immediately, with no buffering, retry, or flush lifecycle.<br>- `span_version` is schema-accepted and defaulted to 2 but never read; v1 has no distinct codec/reporting behavior. |
| Observability / Tracers | [`skywalking`](https://apisix.apache.org/zh/docs/apisix/plugins/skywalking/) | README-listed extra/native | yes | 78% | Supported (README) | - probabilistic `sample_ratio`<br>- `plugin_attr.skywalking` service/instance/endpoint/report_interval defaults<br>- `sw8` parse/injection with 8-field base64url format<br>- random trace/segment IDs and cross-process segment refs<br>- entry/HTTP span with request timing<br>- status/error tags and `$hostname` resolution<br>- `report_interval` buffering and shutdown flush<br>- HTTP segment reporting to `/v3/segments` | - Native OpenResty tracer/shared buffer is absent; official holds context in the `ngx.shared.tracing_buffer` shdict and emits spans from a delayed body-filter hook, while Go buffers segments in a per-plugin mutex slice.<br>- Exact delayed body-filter/streaming timing cannot be reproduced; Go closes the span in the handler middleware after metrics capture. |
| Observability / Tracers | [`opentelemetry`](https://apisix.apache.org/zh/docs/apisix/plugins/opentelemetry/) | README-listed extra/native | yes | 82% | Supported (README) | - official plugin name and priority<br>- sampler names `always_on`, `always_off`, `trace_id_ratio`, `parent_base` with defaults<br>- configurable middleware server name<br>- `additional_attributes` and `additional_header_prefix_attributes`<br>- OTLP/HTTP collector address/timeout/headers<br>- resource attributes with dotted-key flattening<br>- batch processor sizing/timeouts/blocking behavior<br>- `x-request-id` trace IDs<br>- `otel` registered as a compatibility alias | - `set_ngx_var` is schema-accepted but never read; no NGINX-variable equivalent is exposed.<br>- Phase child spans are not modeled; official builds child-span trees across rewrite/access/log phases while Go emits a single middleware span.<br>- Spans close via middleware after the response, not in a log phase, so exact OpenResty log-phase timing does not apply. |
| Observability / Metrics | [`prometheus`](https://apisix.apache.org/zh/docs/apisix/plugins/prometheus/) | APISIX 3.17 default | yes | 82% | Deferred (native/runtime) | - official name, priority, `prefer_name` schema validation<br>- route/service metric labels using IDs by default and names with `prefer_name`<br>- public API metrics endpoint at `/apisix/prometheus/metrics`<br>- status, matched route/host, consumer, balancer labels<br>- request/upstream/APISIX latency histograms<br>- ingress/egress bytes<br>- AI dimensions and LLM latency/tokens/active connections<br>- bounded extra-label variables<br>- `metric_prefix`, `default_buckets`, `llm_latency_buckets`<br>- `export_uri`, `enable_export_server`, `export_addr` | - Metric expiration is absent; client_golang vectors accumulate labels forever, so stale series never age out.<br>- Privileged-agent offload is absent; the metrics endpoint runs as a plain HTTP server in the Go process.<br>- NGINX lifecycle counters are incomplete: `http_current_connections` is never updated and several etcd/upstream series are registered but never populated.<br>- Stream-plane metrics are not collected. |
| Observability / Metrics | [`node-status`](https://apisix.apache.org/zh/docs/apisix/plugins/node-status/) | README-listed extra/native | yes | 78% | Supported (README) | - `/apisix/status` endpoint registration<br>- configured IDs and persisted generated `conf/apisix.uid` IDs<br>- process-wide active/accepted/handled/total HTTP request counters<br>- JSON `id` + `status` response shape | - Exact NGINX reading/writing/waiting connection-state counters remain native; official parses `/apisix/nginx_status` while Go hardcodes zero values, so live connection-state reporting requires a Go-side connection accounting layer. |
| Observability / Metrics | [`datadog`](https://apisix.apache.org/zh/docs/apisix/plugins/datadog/) | APISIX 3.17 default | yes | 88% | Supported (monitor) | - DogStatsD UDP metrics<br>- metadata `host`, `port`, `namespace`, `constant_tags`<br>- route `constant_tags` and `prefer_name`<br>- consumer/balancer/status/scheme tags<br>- optional matched path/method tags<br>- request/upstream/APISIX latency<br>- ingress/egress sizes<br>- APISIX batch processor fields and retry policy<br>- route/server buffering metrics and shutdown flush<br>- datagram-size coalescing with per-line fallback | - Exact OpenResty log-phase timing is not reproduced; Go pushes entries from the handler middleware after metrics capture rather than in a log phase.<br>- Stale batch-manager object-cache behavior has no counterpart; Go creates a fresh processor per route context. |
| Observability / Loggers | [`http-logger`](https://apisix.apache.org/zh/docs/apisix/plugins/http-logger/) | APISIX 3.17 default | yes | 90% | Supported (monitor) | - `uri`, encrypted `auth_header`, `timeout`, `ssl_verify`, HTTP/S POST delivery<br>- route/metadata nested `log_format` with depth-five cap<br>- default route/request/response/consumer fields<br>- `concat_method` JSON/new-line batches<br>- gzip/brotli response-body decoding<br>- `include_req_body` / `include_req_body_expr`<br>- `include_resp_body` / `include_resp_body_expr` with capped capture<br>- batch processor fields (`batch_max_size`, `max_retry_count`, `retry_delay`, `buffer_duration`, `inactive_timeout`)<br>- `max_pending_entries`, graceful reload/shutdown flush, retired/current isolation, capacity recovery<br>- route/server-aware `batch_process_entries` gauge<br>- the standalone corpus executes all 114 pinned source blocks | - Exact OpenResty timer/cache identity and Lua batch-manager stale-object log messages remain deferred; their observable flush, replacement, and capacity-recovery behavior is covered. |
| Observability / Loggers | [`skywalking-logger`](https://apisix.apache.org/zh/docs/apisix/plugins/skywalking-logger/) | APISIX 3.17 default | yes | 76% | Supported (monitor) | - `endpoint_addr`, `service_name`, `service_instance_name`, `timeout`, `log_format`<br>- `/v3/logs` delivery with basic `sw8` trace correlation<br>- `include_req_body` / `include_req_body_expr`<br>- `include_resp_body` / `include_resp_body_expr` with capped capture<br>- batch processor fields<br>- JSON-array batch payloads and `max_pending_entries`<br>- graceful reload/shutdown flush<br>- route/server-aware `batch_process_entries` | - No normal Go TODO; sw8 fidelity beyond header trace context is intentionally bounded. |
| Observability / Loggers | [`tcp-logger`](https://apisix.apache.org/zh/docs/apisix/plugins/tcp-logger/) | APISIX 3.17 default | yes | 70% | Supported (monitor) | - `host`, `port`, `timeout`, `log_format`<br>- `tls`, `tls_options` as TLS server name / SNI<br>- `include_req_body` / `include_req_body_expr`<br>- `include_resp_body` / `include_resp_body_expr` with capped capture<br>- batch processor fields<br>- JSON batch payloads and `max_pending_entries`<br>- graceful reload/shutdown flush<br>- route/server-aware `batch_process_entries` | - No normal Go TODO; OpenResty cosocket pooling/TLS exactness is not required — delivery dials per send with the configured TLS settings. |
| Observability / Loggers | [`kafka-logger`](https://apisix.apache.org/zh/docs/apisix/plugins/kafka-logger/) | APISIX 3.17 default | yes | 76% | Supported (monitor) | - `brokers`, deprecated `broker_list`, `kafka_topic`, `key`, `producer_type`, `required_acks`, `timeout`<br>- broker `sasl_config` for `PLAIN` / `SCRAM-SHA-256` / `SCRAM-SHA-512`<br>- encrypted broker SASL passwords resolved strictly before writer creation; rotated keys supported and invalid ciphertext fails initialization<br>- producer batch defaults and `meta_format = origin`<br>- `log_format`, `include_req_body` / `include_req_body_expr`, `include_resp_body` / `include_resp_body_expr` with capped capture<br>- batch processor fields and single-object / JSON-array batch payloads<br>- `max_pending_entries` | - Exact APISIX Lua batch-manager stale-object cleanup remains deferred; the Go processor flushes on stop and tracks dropped/delivered statistics. |
| Observability / Loggers | [`rocketmq-logger`](https://apisix.apache.org/zh/docs/apisix/plugins/rocketmq-logger/) | APISIX 3.17 default | yes | 72% | Supported (monitor) | - `nameserver_list`, `topic`, `key`, `tag`, `timeout`<br>- `access_key`, encrypted `secret_key`<br>- `meta_format = origin`, `log_format`<br>- `include_req_body` / `include_req_body_expr`<br>- `include_resp_body` / `include_resp_body_expr` with capped capture<br>- batch processor fields and single-object / JSON-array batch payloads<br>- `max_pending_entries` | - TLS remains blocked by the pinned client lacking a public TLS transport hook; `use_tls` is hard-rejected at PostInit, so implement only with an approved client transport contract. |
| Observability / Loggers | [`udp-logger`](https://apisix.apache.org/zh/docs/apisix/plugins/udp-logger/) | APISIX 3.17 default | yes | 70% | Supported (monitor) | - `host`, `port`, `timeout`, `log_format`, UDP delivery<br>- `include_req_body` / `include_req_body_expr`<br>- `include_resp_body` / `include_resp_body_expr` with capped capture<br>- batch processor fields<br>- JSON batch payloads and `max_pending_entries`<br>- graceful reload/shutdown flush<br>- route/server-aware `batch_process_entries` | - No normal Go TODO; per-send UDP dials leave cosocket-pool parity out of scope. |
| Observability / Loggers | [`clickhouse-logger`](https://apisix.apache.org/zh/docs/apisix/plugins/clickhouse-logger/) | APISIX 3.17 default | yes | 76% | Supported (monitor) | - `endpoint_addr`, random `endpoint_addrs` selection<br>- `user`, encrypted `password`, `database`, `logtable`, `timeout`, `ssl_verify`, `log_format`<br>- `include_req_body` / `include_req_body_expr`<br>- `include_resp_body` / `include_resp_body_expr` with capped capture<br>- batch processor fields<br>- ClickHouse JSONEachRow batch payloads and `max_pending_entries`<br>- graceful reload/shutdown flush<br>- route/server-aware `batch_process_entries` | - Exact APISIX Lua batch-manager stale-object cleanup remains deferred; Go relies on processor stop-flush and per-route instances. |
| Observability / Loggers | [`syslog`](https://apisix.apache.org/zh/docs/apisix/plugins/syslog/) | APISIX 3.17 default | yes | 70% | Supported (monitor) | - exact raw RFC5424 SYSLOG/INFO frames (`<46>1`, millisecond UTC timestamp, host, `apisix`, PID, JSON, newline)<br>- TCP, UDP, and TLS delivery with ordered frame concatenation<br>- socket-byte buffering through `flush_limit` / `drop_limit`<br>- write-bounded `timeout` and `pool_size`-bounded reusable connections<br>- route/metadata `log_format` / `log_format_extra` precedence with nested objects and depth-five truncation<br>- `include_req_body` / `include_req_body_expr`, `include_resp_body` / `include_resp_body_expr` with size caps<br>- batch processor fields and `max_pending_entries`<br>- graceful reload/shutdown flush<br>- route/server-aware `batch_process_entries` | - OpenResty cosocket's worker-shared keepalive pool and timer scheduling remain native lifecycle boundaries; the Go implementation uses a per-route bounded idle pool and the shared Go batch processor. |
| Observability / Loggers | [`log-rotate`](https://apisix.apache.org/zh/docs/apisix/plugins/log-rotate/) | README-listed extra/native | yes | 72% | Supported (README) | - `plugin_attr.log-rotate` defaults for `interval`, `max_kept`, `max_size`, `timeout`, `enable_compression`<br>- APISIX timestamped `__access.log` / `__error.log` naming<br>- max-size rotation and interval checks on request<br>- current-file recreation and Go-side file-logger path reopen after rotation<br>- history pruning and `.tar.gz` compression | - OpenResty timer lifecycle, config discovery, and system-`tar` parity remain deferred.<br>- SIGUSR1-based file reopening is implemented in the file-logger registry; the deferral covers only NGINX master-process signal parity. |
| Observability / Loggers | [`error-log-logger`](https://apisix.apache.org/zh/docs/apisix/plugins/error-log-logger/) | README-listed extra/native | yes | 69% | Supported (README) | - official metadata-shaped config for `tcp`, `skywalking`, `clickhouse`, `kafka`<br>- legacy `host` / `port` TCP config<br>- level filtering with observer replacement<br>- TCP/TLS delivery, SkyWalking `/v3/logs` with `$hostname` resolution, ClickHouse JSONEachRow, Kafka topic/key publishing<br>- Kafka broker `sasl_config` with `PLAIN`<br>- encrypted clickhouse/kafka passwords<br>- official batch/default knobs and shared batch processor buffering/retry<br>- graceful reload/shutdown flush | - Direct `ngx.errlog`, OpenResty timer lifecycle, global explicit-delivery labels, stale batch-manager cleanup, and Lua-resty-kafka cache exactness remain deferred; Go observes its own logger instead. |
| Observability / Loggers | [`sls-logger`](https://apisix.apache.org/zh/docs/apisix/plugins/sls-logger/) | APISIX 3.17 default | yes | 72% | Supported (monitor) | - RFC5424 SLS log messages over TLS TCP<br>- `host`, `port`, `project`, `logstore`, `access_key_id`, encrypted `access_key_secret`, `timeout`, `log_format`<br>- `include_req_body` / `include_req_body_expr`<br>- `include_resp_body` / `include_resp_body_expr` with capped capture<br>- batch processor fields<br>- concatenated RFC5424 batch writes<br>- graceful reload/shutdown flush<br>- route/server-aware `batch_process_entries` | - Exact APISIX Lua batch-manager stale-object cleanup remains deferred; the Go processor flushes on stop and drops past limits with logged counters. |
| Observability / Loggers | [`google-cloud-logging`](https://apisix.apache.org/zh/docs/apisix/plugins/google-cloud-logging/) | APISIX 3.17 default | yes | 67% | Supported (monitor) | - service-account `auth_config` / `auth_file` with encrypted private key<br>- JWT bearer token exchange with cached, skew-aware refresh<br>- `entries_uri`, `resource`, `log_id`, `ssl_verify`, `log_format`<br>- default Cloud Logging `httpRequest` entry expansion<br>- batch processor fields<br>- multi-entry writes and `max_pending_entries` | - Request/response body capture is outside the APISIX 3.17 configuration surface, confirmed absent in the pinned official source; exact batch-manager behavior remains deferred. |
| Observability / Loggers | [`splunk-hec-logging`](https://apisix.apache.org/zh/docs/apisix/plugins/splunk-hec-logging/) | APISIX 3.17 default | yes | 62% | Supported (monitor) | - `endpoint.uri`, encrypted `endpoint.token`, `endpoint.channel`, `endpoint.timeout`, `endpoint.keepalive_timeout`<br>- `ssl_verify`, `log_format`, `log_format_extra`<br>- batch processor fields<br>- concatenated JSON event-object batch payloads and `max_pending_entries`<br>- HEC error-text extraction | - Request/response body capture is outside the APISIX 3.17 configuration surface, confirmed absent in the pinned official source; exact batch-manager behavior remains deferred. |
| Observability / Loggers | [`file-logger`](https://apisix.apache.org/zh/docs/apisix/plugins/file-logger/) | APISIX 3.17 default | yes | 82% | Supported (monitor) | - `path` from route or metadata<br>- `log_format` / `log_format_extra` precedence with depth-five cap<br>- `match` expression filtering<br>- `include_req_body` / `include_req_body_expr`, `include_resp_body` / `include_resp_body_expr` with size caps<br>- gzip/brotli response-body decoding<br>- zap JSON writes through a shared per-path writer registry<br>- SIGUSR1 reopen of cached writers and log-rotate flush/reopen hook<br>- shutdown flush | - Exact APISIX/OpenResty file-cache semantics remain deferred; the Go registry uses lease-counted shared writers and signal-driven reopen rather than OpenResty lrucache expiry. |
| Observability / Loggers | [`loggly`](https://apisix.apache.org/zh/docs/apisix/plugins/loggly/) | APISIX 3.17 default | yes | 76% | Supported (monitor) | - RFC5424 Loggly syslog messages over UDP and HTTP/S bulk endpoint delivery<br>- encrypted `customer_token` with standalone encryption at rest and plugin-boundary decryption<br>- `severity`, response-status `severity_map`, `tags`, `host`, `port`, `protocol`, `timeout`, `ssl_verify`<br>- metadata/route `log_format` precedence with route identity<br>- `include_req_body` / `include_req_body_expr`, `include_resp_body` / `include_resp_body_expr` with capped capture<br>- batch processor fields<br>- HTTP/S newline bulk batching and UDP per-entry delivery<br>- metadata delivery config fallback and `max_pending_entries`<br>- graceful reload/shutdown flush<br>- route/server-aware `batch_process_entries`<br>- the standalone corpus executes all 22 pinned source blocks | - Exact APISIX Lua batch-manager stale-object cleanup remains deferred; per-instance buffered config closures exist by construction but lack a dedicated test name. |
| Observability / Loggers | [`elasticsearch-logger`](https://apisix.apache.org/zh/docs/apisix/plugins/elasticsearch-logger/) | APISIX 3.17 default | yes | 84% | Supported (monitor) | - `endpoint_addr`, random `endpoint_addrs` selection<br>- `field.index` with time and APISIX variable expansion<br>- major-version discovery with `_type = _doc` only for versions 5/6, ignored `field.type` on modern versions<br>- `auth`, `headers`, `timeout`, `ssl_verify`, `log_format`<br>- `include_req_body` / `include_req_body_expr`, `include_resp_body` / `include_resp_body_expr` with capped capture<br>- batch processor fields<br>- `_bulk` NDJSON delivery with `application/x-ndjson`<br>- `max_pending_entries`, graceful reload/shutdown flush<br>- route/server-aware `batch_process_entries`<br>- standalone plaintext `auth.password` and `headers.Authorization` encrypted at rest and decrypted for runtime sink authentication<br>- the standalone corpus executes all 27 pinned `elasticsearch-logger.t` source blocks plus 8 from `elasticsearch-logger2.t` | - Exact APISIX Lua batch-manager stale-object cleanup remains deferred; Go processor flush/retry semantics cover the observable behavior. |
| Observability / Loggers | [`tencent-cloud-cls`](https://apisix.apache.org/zh/docs/apisix/plugins/tencent-cloud-cls/) | APISIX 3.17 default | yes | 76% | Supported (monitor) | - `cls_host`, `cls_topic`, `scheme`, `ssl_verify`<br>- encrypted `secret_id` / `secret_key`<br>- `sample_ratio`, `global_tag`, `log_format`<br>- `include_req_body` / `include_req_body_expr`, `include_resp_body` / `include_resp_body_expr` with capped capture<br>- Tencent CLS sha1 authorization and `/structuredlog` protobuf delivery<br>- batch processor fields<br>- multi-log protobuf batch payloads and `max_pending_entries`<br>- graceful reload/shutdown flush<br>- route/server-aware `batch_process_entries` | - Exact APISIX Lua batch-manager cleanup remains deferred; upstream SDK lz4/zstd TODO is not a plugin feature, and the pinned official source confirms no compression surface. |
| Observability / Loggers | [`loki-logger`](https://apisix.apache.org/zh/docs/apisix/plugins/loki-logger/) | APISIX 3.17 default | yes | 76% | Supported (monitor) | - random `endpoint_addrs` selection, `endpoint_uri`, `tenant_id`<br>- custom headers and `log_labels` with variable resolution<br>- `ssl_verify`, `timeout`, keepalive defaults<br>- route/metadata `log_format` / `log_format_extra`<br>- `include_req_body` / `include_req_body_expr`, `include_resp_body` / `include_resp_body_expr` with capped capture<br>- batch processor fields<br>- one-stream multi-value Loki batches and `max_pending_entries`<br>- graceful reload/shutdown flush<br>- route/server-aware `batch_process_entries` | - Exact APISIX Lua batch-manager stale-object cleanup remains deferred; Go groups entries per label-key stream and flushes on stop. |
| Observability / Loggers | [`lago`](https://apisix.apache.org/docs/apisix/plugins/lago/) | APISIX 3.17 default | yes | 76% | Supported (monitor) | - official name, priority, schema and Lago batch event payload shape<br>- random `endpoint_addrs` selection with Bearer token delivery to `endpoint_uri`<br>- request/response variable templates for transaction ID, subscription ID, event properties<br>- request-start event timestamps and dynamic `${arg_*}`, `${cookie_*}`, `${http_*}`, `${sent_http_*}`, `${upstream_http_*}` variables<br>- `include_req_body` / `include_resp_body` with capped capture through `${request_body}` / `${response_body}` templates<br>- `ssl_verify`, timeout, keepalive defaults<br>- APISIX `batch_max_size` default of 100<br>- batch processor fields | - Exotic OpenResty/NGINX-only variable fidelity remains deferred; common template classes resolve, with fallback to the Go variable store. |
| AI | [`ai`](https://github.com/apache/apisix/blob/release/3.17/apisix/plugins/ai.lua) | APISIX 3.17 default | yes | 20% | Deferred (native/runtime) | - official name, priority, empty schema<br>- pass-through HTTP handler, no-op registration | - Keep deferred unless a Go-native replacement for the APISIX router/balancer/runtime replacement is explicitly designed; the official ai.lua hooks the router, balancer, global rules, events, and lrucache. |
| AI | [`ai-aliyun-content-moderation`](https://apisix.apache.org/docs/apisix/plugins/ai-aliyun-content-moderation/) | APISIX 3.17 default | yes | 96% | Deferred (native/runtime) | - request and response moderation with protocol-aware content extraction<br>- `stream_check_mode` realtime/final_packet with cache size and interval<br>- Aliyun TextModerationPlus HMAC-SHA1 signing with session nonce reuse<br>- `risk_level_bar`, `deny_code`, `deny_message`, `check_request`, `check_response`<br>- protocol-shaped deny wire responses including streaming SSE<br>- `$llm_content_risk_level` variable registration | - Exact OpenResty body-filter chunk timing remains deferred; official performs response moderation at chunk granularity while Go batches on cache-size/interval/final-packet. |
| AI | [`ai-aws-content-moderation`](https://apisix.apache.org/docs/apisix/plugins/ai-aws-content-moderation/) | APISIX 3.17 default | yes | 95% | Supported (monitor) | - AWS Comprehend request checks<br>- SigV4 with static and session credentials<br>- encrypted secrets<br>- endpoint override and TLS controls<br>- category and toxicity thresholds<br>- request replay upstream after check<br>- denial with Go extensions `deny_code` and protocol-shaped deny responses | - Exact `resty.aws` module/cache lifecycle remains deferred; Lua reuses one shared `resty.aws` instance with cached credentials while Go builds its own SigV4 client per plugin instance.<br>- No APISIX 3.17 schema field is missing; `check_request` and `deny_message` are not fields in the pinned 3.17 plugin. |
| AI | [`ai-rag`](https://apisix.apache.org/docs/apisix/plugins/ai-rag/) | APISIX 3.17 default | yes | 98% | Supported (monitor) | - `embeddings_provider.azure_openai` and `vector_search_provider.azure_ai_search` schema<br>- body `ai_rag` field validation<br>- embeddings request and vector search with `api-key` auth<br>- search result injected as a user message via protocol append<br>- `ssl_verify` transport control | - Exact OpenResty HTTP-client lifecycle remains deferred; Lua uses the shared `resty.http` client with worker-level lifecycle while Go owns a per-instance client. |
| AI | [`ai-prompt-decorator`](https://apisix.apache.org/docs/apisix/plugins/ai-prompt-decorator/) | APISIX 3.17 default | yes | 90% | Supported (monitor) | - `prepend` / `append` prompt arrays with role/content schema<br>- protocol detection and message injection<br>- JSON body rewrite and replace | - Exact OpenResty phase/runtime behavior remains deferred; Lua runs in the rewrite phase and mutates via `ngx.req.set_body_data` while Go runs in the middleware chain. |
| AI | [`ai-prompt-guard`](https://apisix.apache.org/docs/apisix/plugins/ai-prompt-guard/) | APISIX 3.17 default | yes | 88% | Supported (monitor) | - allow/deny regex policy evaluation with compiled patterns<br>- shared extraction across all six AI protocols<br>- nested content blocks via Responses API parts<br>- `match_all_roles`, `match_all_conversation_history`, `fail_mode`<br>- official generic rejection responses with fixed messages | - Exact OpenResty regex-engine flags/behavior remain deferred; Lua uses PCRE `re_find` with "jou" flags while Go compiles RE2. |
| AI | [`ai-prompt-template`](https://apisix.apache.org/docs/apisix/plugins/ai-prompt-template/) | APISIX 3.17 default | yes | 95% | Deferred (native/runtime) | - named `templates` array with model/messages<br>- `template_name` body lookup with official missing/unknown-template errors<br>- `{{ expr }}` rendering with nested dotted and bounded indexed lookup<br>- request body replacement and continuation upstream | - Exact OpenResty template LRU behavior remains deferred; official caches template resolution in a templates lrucache while Go does linear scans. |
| AI | [`ai-request-rewrite`](https://apisix.apache.org/docs/apisix/plugins/ai-request-rewrite/) | APISIX 3.17 default | yes | 90% | Supported (monitor) | - `prompt` / `provider` / `auth` schema with a ten-provider enum<br>- LLM call and body replacement with extracted text<br>- AWS SigV4 and GCP token auth paths<br>- `$llm_request_body` / `$ai_request_body_changed` variable registration<br>- timeout, keepalive, `ssl_verify` controls | - Only the exact APISIX error/log lifecycle remains deferred; official fully buffers the LLM response with no streaming path, and Go mirrors that behavior. |
| AI | [`ai-rate-limiting`](https://apisix.apache.org/docs/apisix/plugins/ai-rate-limiting/) | APISIX 3.17 default | yes | 95% | Supported (monitor) | - global, per-instance, and rule quotas<br>- string variable quotas with `${var ?? default}`<br>- quota headers matching Lua names<br>- all four token strategies and bounded `cost_expr`<br>- two-phase pre-check then post-response charge<br>- rate-aware multi-instance fallback<br>- non-buffering streaming usage charging<br>- local, Redis, and Redis Sentinel policies with atomic fixed windows<br>- encrypted Redis/Sentinel credentials | - Exact OpenResty log-phase timing and cross-worker shared local-memory counters remain deferred; Lua charges during the log phase and shares `resty.limit.count` state across workers while Go charges inline with per-process counters. |
| AI | [`ai-proxy`](https://apisix.apache.org/docs/apisix/plugins/ai-proxy/) | APISIX 3.17 default | yes | 99% | Supported (monitor) | - six-protocol detection including passthrough<br>- Anthropic-to-OpenAI request/response/SSE conversion with tools<br>- OpenAI Responses and Embeddings<br>- Vertex embeddings conversion both directions<br>- provider auth header/query/GCP/AWS SigV4<br>- provider endpoints and per-protocol request-body overrides<br>- SSE forwarding with flush interval writer<br>- CRC-validated Bedrock EventStream<br>- usage/timing/upstream variables<br>- `logging.summaries` / `logging.payloads`<br>- `max_stream_duration_ms` / `max_response_bytes` limits<br>- active LLM connection gauge<br>- two-phase rate limiting | - Exact OpenResty transport, phase, and log lifecycle remain deferred; Lua relies on `resty.http`, rewrite-phase execution, and worker log-phase callbacks that Go approximates with its own client and middleware chain. |
| AI | [`ai-proxy-multi`](https://apisix.apache.org/docs/apisix/plugins/ai-proxy-multi/) | APISIX 3.17 default | yes | 98% | Deferred (separate subsystem) | - weighted priorities with descending sort<br>- round-robin/chash across vars/header/cookie/consumer/variable-combination keys with remote-address fallback<br>- HTTP/rate fallback and retries with `max_retries` / `retry_on_failure_within_ms`<br>- active health schema with threshold/interval behavior<br>- all-unhealthy fallback<br>- shared protocol conversion, provider auth, Vertex embeddings, Anthropic SSE, Bedrock EventStream<br>- logging summaries/payloads and stream-duration/flush controls<br>- timing/active metrics and two-phase rate limiting | - Explicit per-address DNS snapshots and per-IP health state require a new transport/health contract; official resolves each instance domain into per-IP nodes and tracks per-node health, while Go keeps one health state per instance index with no DNS resolution. |
| AI | [`mcp-bridge`](https://github.com/apache/apisix/blob/release/3.17/apisix/plugins/mcp-bridge.lua) | APISIX 3.17 default | yes | 75% | Deferred (native/runtime) | - official name, priority, schema, configurable `base_uri`<br>- stdio subprocess launch with `command` / `args`<br>- `GET {base_uri}/sse` with advertised message endpoints<br>- 30-second JSON-RPC ping keepalives<br>- `POST {base_uri}/message` forwarding<br>- cancellation-aware stdout/stderr line forwarding<br>- `notifications/stderr`<br>- subprocess cleanup on disconnect | - Cross-worker shared-dict session recovery, exact `ngx.pipe` timeout/worker-exit semantics, and distributed state remain deferred; Go uses `os/exec` with in-process session maps. |
| Stream | [`mqtt-proxy`](https://apisix.apache.org/docs/apisix/plugins/mqtt-proxy/) | Registered non-default | yes | 64% | Deferred (separate subsystem) | - official name, priority, schema, required `protocol_level`<br>- default `protocol_name = "MQTT"`<br>- MQTT 5 CONNECT decoded with Eclipse Paho packets; MQTT 3.1/3.1.1 with the local bounded parser<br>- flags validation and UTF-8 validation with zero-byte rejection<br>- client-ID extraction with peer-address fallback<br>- plugin-owned `ServeStream` preread/replay with bidirectional cancellation<br>- plugin-owned `ServeListener` accept loop with `StreamInfo` callbacks<br>- main-server TCP route binding with weighted/chash upstream selection, route-owned idle timeout, and transactional startup | - The main stream owner is deliberately bounded to raw TCP and `mqtt-proxy`; empty TCP listeners, UDP, TLS/mTLS, listener/upstream or top-level PROXY protocol, unresolved upstreams, unsupported stream plugins, and bind failures are rejected at startup.<br>- General stream-variable/plugin-chain support, stream metrics, and dynamic readiness publication require a separate stream contract; no readiness endpoint is provided today. |
| Development | [`example-plugin`](https://github.com/apache/apisix/blob/release/3.17/apisix/plugins/example-plugin.lua) | APISIX 3.17 default | yes | 80% | Supported (monitor) | - official name, priority, schema with required `i` and optional `s` / `t` / `ip` / `port`<br>- metadata schema<br>- pass-through middleware<br>- route upstream override through the traffic-split path when `ip` is configured<br>- control API `GET /v1/plugin/example-plugin/hello` with text or JSON response | - OpenResty phase-specific logging, delayed body-filter timing, and direct `apisix.upstream.set` parity remain demo/native-only; Go approximates the upstream override through the traffic-split path. |

## Cross-plugin remaining work

These items require a new owner or runtime contract and should not be implemented as disconnected plugin-local work:

1. **General stream-plugin context:** define a stream context and lifecycle owner for every supported stream plugin, then add end-to-end fixtures. The current stream design is documented in [`design.md`](design.md).
2. **Active upstream health probes and health-status API:** define scheduler ownership, state persistence, and `/v1/healthcheck` semantics before implementation.
3. **Kafka external-broker smoke coverage:** add only when an external integration environment and credential-safe CI contract are available.
4. **gRPC streaming and cancellation:** expose an explicit route binding before adding a streaming response owner.
5. **Concrete expression, regex, or schema mismatches:** reproduce the APISIX-vs-Go mismatch first, then add the smallest regression and fix.

## Scope boundary

The following remain intentionally outside normal parity work: arbitrary Lua execution, `ngx_lua` APIs, exact OpenResty phase and body-filter timing, NGINX buffering and connection-state internals, OCSP/TLS stapling internals, exact shared-dict/lrucache/batch-manager behavior, OpenResty cosocket pooling, and external plugin-runner protocol compatibility.
