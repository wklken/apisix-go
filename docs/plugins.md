# APISIX 3.17 Plugin Support

> Current Go-native plugin status, supported behavior, and remaining compatibility boundaries.
>
> Official comparison baseline: Apache APISIX `3.17.0` at `9ef2ecab67f652d38365049613610ef649bb4ad0` (104 default plugins). Evidence is maintained against `pkg/plugin/init.go`, the plugin packages, route/proxy integration, focused tests, and the repository verification gate.
>
> Status audit: 2026-08-21. HTTP plugin request/response/streaming/protocol/log
> ownership is explicit for all 115 factory keys / 114 implementation identities;
> direct `Handler` compatibility paths are not installed by production routes.

## Summary

- Official APISIX 3.17 default plugins: **104**
- Go-applicable default plugins: **99**; all **99** are registered (**100%**)
- Config-and-behavior compatibility among applicable defaults: **73 full**, **26 partial**
- Runtime-specific defaults with no meaningful Go plugin equivalent: **5 N/A**
- Integration-corpus gate: **100 rows** (98 source-backed rows plus 2 explicit upstream-source absences)
- Additional documented rows: **14** (12 README/native extras and 2 registered non-default protocol entries)
- Total plugin rows in the status table: **118**

## Integrated plugin tests

The standalone integration suite under [`t/plugin`](../t/plugin/README.md) is
pinned to Apache APISIX commit
`c3d7d5ec69774121f53d2e20d29d09c816795dd7`. It contains 99 manifest files for
the 98 source-backed rows whose **Corpus gate** is `yes` plus supplemental
`redirect2`. Every manifest has at least one scenario that activates its target
plugin and passes the semantic coverage gate. Intentional negative scenarios
that keep it disabled require a nonblank, non-stale
`target_plugin_exempt_reason`. Every scenario writes fresh
`config.yaml` and `apisix.yaml`, starts the real APISIX-Go CLI in standalone
YAML-provider mode, sends requests, and asserts the response, fixture request,
or startup log. External services are replaced by deterministic per-case
loopback fixtures, including TCP/TLS-TCP, UDP, gRPC, Redis, Kafka, Dubbo, and
LDAP protocol fixtures; no placeholder or skip reason is treated as coverage.

The four intentionally unregistered defaults are `ext-plugin-pre-req`, `ext-plugin-post-req`, `ext-plugin-post-resp`, and `inspect`. They require an external plugin runner or inspect LuaJIT/OpenResty itself, so they are `N/A` rather than unsupported Go behavior. The `ai` module is also `N/A`: APISIX uses it as a Lua router/balancer coordinator, while Go owns those responsibilities in its native pipeline. The bounded `serverless-pre-function` and `serverless-post-function` implementations remain `Partial` because user-provided Lua can call APIs that the compatibility runtime does not implement.

## Compatibility definitions

- **100%:** the APISIX 3.17 configuration surface and externally observable behavior have no known mismatch. Equivalent Go mechanisms count as compatible; implementation identity is not required.
- **Partial:** a concrete configuration value or observable result is missing or behaves differently. The table names that gap instead of assigning an unverifiable percentage.
- **N/A:** the row describes an OpenResty/NGINX/external-runner facility with no meaningful Go plugin contract. It is excluded from the Go-applicable denominator, not reported as unsupported behavior.
- **Corpus gate:** `yes` means the row participates in `t/plugin` manifest selection; `no` means it currently has focused coverage only or is `N/A`. Corpus conversion is verification evidence, not a feature-status decision.
- **Known behavior gap:** only configurable or externally observable differences belong here. shared-dict/lrucache/cosocket choices, exact phase timing, worker topology, and NGINX-only counters do not reduce compatibility unless they change the promised behavior.

## Plugin status

The table below is the single source of truth for plugin compatibility. Behavior cells summarize implemented surfaces; the final column records only known configuration or observable-behavior gaps. Source and focused tests hold the exhaustive detail.

| Category | Plugin | APISIX 3.17 scope | Registered | Config + behavior | Corpus gate | Go behavior | Known behavior gap |
|---|---|---|---:|---:|---|---|---|
| General | [`batch-requests`](https://apisix.apache.org/zh/docs/apisix/plugins/batch-requests/) | README-listed extra/native | yes | 100% | yes | - POST `/apisix/batch-requests` with `plugin_attr.batch-requests.uri` override<br>- global and per-item query/header merging while pinning inner host identity to the outer request<br>- path-only inner targets<br>- HTTP version validation and default methods<br>- request body forwarding | - None. |
| General | [`redirect`](https://apisix.apache.org/zh/docs/apisix/plugins/redirect/) | APISIX 3.17 default | yes | Partial | yes | - `uri` rewriting with `$var` / `${var}` / `\$` expansion<br>- `regex_uri` with compile-time validation<br>- `http_to_https` with forwarded HTTPS detection<br>- method-specific 301/308 responses and query appending without an empty trailing `?` | - The official `uri` pattern constraint is not enforced during config validation. |
| General | [`echo`](https://apisix.apache.org/zh/docs/apisix/plugins/echo/) | APISIX 3.17 default | yes | 100% | yes | - `before_body`, `body`, `after_body` rewriting<br>- response `headers` with string or numeric values<br>- body-modification header cleanup<br>- official body/header schema boundaries | - None. |
| General | [`gzip`](https://apisix.apache.org/zh/docs/apisix/plugins/gzip/) | APISIX 3.17 default | yes | 100% | yes | - `types` and `types = "*"` wildcard matching<br>- `min_length` gating when the response provides `Content-Length`<br>- `comp_level`, `http_version`, and `vary`<br>- gzip-only offer; shared `Accept-Encoding` negotiation honors q-values, preference order, and `q=0` | - None. |
| General | [`brotli`](https://apisix.apache.org/zh/docs/apisix/plugins/brotli/) | README-listed extra/native | yes | 100% | yes | - `Accept-Encoding` recognition for `br` / `*` with basic `q=0` disabling<br>- `types`, `min_length`, `comp_level`, `lgwin`, `http_version`, `vary`<br>- content-encoding skip and content-length removal<br>- strong ETag weakening | - None. |
| General | [`real-ip`](https://apisix.apache.org/zh/docs/apisix/plugins/real-ip/) | APISIX 3.17 default | yes | 100% | yes | - `arg_*`, `http_*`, `cookie_*` source variables require configured `trusted_addresses` before rewriting client identity<br>- `http_x_forwarded_for` recursive trusted-CIDR walk<br>- common built-in request variables<br>- bare IP and IP:port sources with valid port bounds | - None. |
| General | [`server-info`](https://apisix.apache.org/zh/docs/apisix/plugins/server-info/) | README-listed extra/native | yes | Partial | yes | - `/v1/server_info` response shape when enabled in `conf.plugins`<br>- configured `apisix.id` node IDs<br>- persisted `conf/apisix.uid` fallback IDs<br>- generated process fallback IDs | - The public response reports the etcd version as unknown instead of resolving the live version. |
| General | [`error-page`](https://apisix.apache.org/zh/docs/apisix/plugins/error-page/) | README-listed extra/native | yes | 100% | yes | - metadata-shaped `enable` and `error_404` / `error_500` / `error_502` / `error_503`<br>- custom body / content-type / content-length<br>- default APISIX-style HTML bodies<br>- upstream-sourced responses skipped via `$response_source` | - None. |
| General | [`exit-transformer`](https://apisix.apache.org/zh/docs/apisix/plugins/exit-transformer/) | README-listed extra/native | yes | Partial | yes | - chained response capture<br>- sandboxed Lua control flow and mutation<br>- status remap and normalized JSON errors<br>- upstream-response provenance guard | - Configured callbacks that depend on `core.response.exit()` or unsupported `ngx_lua` APIs cannot run. |
| General | [`attach-consumer-label`](https://apisix.apache.org/zh/docs/apisix/plugins/attach-consumer-label/) | APISIX 3.17 default | yes | 100% | yes | - client-supplied header deletion before label mapping<br>- authenticated consumer label copy into request headers<br>- JSON serialization for numeric, boolean, and array label values<br>- no consumer or no labels leaves client headers untouched | - None. |
| General | [`serverless-pre-function`](https://apisix.apache.org/zh/docs/apisix/plugins/serverless/) | APISIX 3.17 default | yes | Partial | no | - Lua chunks returning functions with sequential execution<br>- `code` / `body` return short-circuiting<br>- `ngx.log`, `ngx.say`, `ngx.req.set_header`, `ngx.header`, `ngx.status`, `ngx.arg`<br>- `cjson` and selected `apisix.core` helpers | - User Lua that depends on `ngx.ctx`, functional `ngx.log`, custom variables, or other unavailable `ngx_lua` APIs behaves differently. |
| General | [`serverless-post-function`](https://apisix.apache.org/zh/docs/apisix/plugins/serverless/) | APISIX 3.17 default | yes | Partial | no | - request-phase execution<br>- response capture for `header_filter` / `body_filter` / `log`<br>- response header/status/body mutation<br>- documented JSON body-filter rewrite pattern | - User Lua that depends on streaming body chunks, custom variables, or unavailable `ngx_lua` APIs behaves differently after the buffered response. |
| General | [`azure-functions`](https://apisix.apache.org/zh/docs/apisix/plugins/azure-functions/) | APISIX 3.17 default | yes | 100% | yes | - method/query/body/header forwarding with wildcard `:ext` paths<br>- client/route/metadata authorization precedence<br>- encrypted API keys and header injection<br>- shared progress-timeout transport with streamed response/error accounting | - None. |
| General | [`openfunction`](https://apisix.apache.org/zh/docs/apisix/plugins/openfunction/) | APISIX 3.17 default | yes | 100% | yes | - method/query/body/header forwarding with wildcard `:ext` paths<br>- encrypted Basic authorization<br>- status/body/header relaying with HTTP/2 filtering<br>- shared progress-timeout transport with streamed response/error accounting | - None. |
| General | [`openwhisk`](https://apisix.apache.org/zh/docs/apisix/plugins/openwhisk/) | APISIX 3.17 default | yes | 100% | yes | - action endpoint construction with optional package<br>- POST body forwarding with Basic auth from encrypted `service_token`<br>- default `blocking` / `result` / `timeout` query parameters<br>- JSON result `statusCode` / scalar-or-list `headers` / body values, with `body: false` falling back to the original JSON response | - None. |
| General | [`aws-lambda`](https://apisix.apache.org/zh/docs/apisix/plugins/aws-lambda/) | APISIX 3.17 default | yes | 100% | yes | - method/query/body/header forwarding with wildcard `:ext` paths<br>- encrypted API-key/IAM credentials and SigV4 signing<br>- status/body/header relaying with HTTP/2 filtering<br>- shared progress-timeout transport with streamed response/error accounting | - None. |
| General | [`ext-plugin-pre-req`](https://apisix.apache.org/zh/docs/apisix/plugins/ext-plugin-pre-req/) | APISIX 3.17 default | no | N/A | no | - None; requires an external plugin runner. | - N/A: this is an external plugin-runner protocol stage, not a Go-native plugin contract. |
| General | [`ext-plugin-post-req`](https://apisix.apache.org/zh/docs/apisix/plugins/ext-plugin-post-req/) | APISIX 3.17 default | no | N/A | no | - None; requires an external plugin runner. | - N/A: this is an external plugin-runner protocol stage, not a Go-native plugin contract. |
| General | [`ext-plugin-post-resp`](https://apisix.apache.org/zh/docs/apisix/plugins/ext-plugin-post-resp/) | APISIX 3.17 default | no | N/A | no | - None; requires an external plugin runner. | - N/A: this is an external plugin-runner protocol stage, not a Go-native plugin contract. |
| General | [`inspect`](https://apisix.apache.org/zh/docs/apisix/plugins/inspect/) | APISIX 3.17 default | no | N/A | no | - None; requires a Lua/OpenResty inspection runtime. | - N/A: the official module instruments LuaJIT garbage collection and OpenResty phase hooks. |
| General | [`ocsp-stapling`](https://apisix.apache.org/zh/docs/apisix/plugins/ocsp-stapling/) | README-listed extra/native | no | N/A | no | - None; TLS-listener native behavior. | - N/A: certificate-chain validation, OCSP caching, and handshake stapling belong to the TLS listener. |
| Transformation | [`response-rewrite`](https://apisix.apache.org/zh/docs/apisix/plugins/response-rewrite/) | APISIX 3.17 default | yes | Partial | yes | - `status_code` and validated plain/base64 `body`<br>- legacy and `add` / `set` / `remove` headers with string or numeric values<br>- response/request header variables<br>- nested `lua-resty-expr` logical groups | - Configured PCRE-only patterns, named captures, backtracking, or Lua `gsub` flag semantics can differ under Go RE2. |
| Transformation | [`proxy-rewrite`](https://apisix.apache.org/zh/docs/apisix/plugins/proxy-rewrite/) | APISIX 3.17 default | yes | Partial | yes | - URI/regex rewriting with early method finalization<br>- unsafe raw-URI compatibility path<br>- host/scheme override<br>- header operations with variables and captures | - PCRE-only `regex_uri`, exact OpenResty URI normalization, and capture-context variables can produce different rewrites. |
| Transformation | [`grpc-transcode`](https://apisix.apache.org/zh/docs/apisix/plugins/grpc-transcode/) | APISIX 3.17 default | yes | Partial | no | - cached `.pb`/`.proto` bindings with imports<br>- GET/POST JSON-to-protobuf mapping<br>- unary gRPC framing, integer deadlines, trailers, and HTTP status mapping<br>- enum/int64/default/detail JSON options | - `pb_option = enable_hooks` is accepted but Lua protobuf hooks are not executed. |
| Transformation | [`grpc-web`](https://apisix.apache.org/zh/docs/apisix/plugins/grpc-web/) | APISIX 3.17 default | yes | Partial | no | - CORS/preflight and wildcard route rewriting<br>- binary/text request translation<br>- streamed binary responses with cancellation<br>- response/trailer framing and content-type restoration | - Text responses encode each write and trailer independently instead of using one response-lifetime Base64 stream. |
| Transformation | [`fault-injection`](https://apisix.apache.org/zh/docs/apisix/plugins/fault-injection/) | APISIX 3.17 default | yes | Partial | yes | - `abort` and `delay`<br>- omitted-vs-explicit `percentage`<br>- empty abort bodies<br>- string/numeric response headers | - Expression rules that require PCRE-only syntax or NGINX variables outside the Go catalog can differ. |
| Transformation | [`mocking`](https://apisix.apache.org/zh/docs/apisix/plugins/mocking/) | APISIX 3.17 default | yes | Partial | yes | - `response_example` preferred over `response_schema`<br>- schema object generation with `example` values<br>- JSON/plain-text/XML schema bodies<br>- response headers with bounded variable resolution | - Example-less string schemas produce an empty string instead of the official random lowercase value. |
| Transformation | [`degraphql`](https://apisix.apache.org/zh/docs/apisix/plugins/degraphql/) | APISIX 3.17 default | yes | 100% | yes | - bounded GraphQL syntax/structure validation<br>- multiple-operation `operation_name` enforcement<br>- GET/POST rewriting to `query`, `variables`, `operationName` | - None. |
| Transformation | [`body-transformer`](https://apisix.apache.org/zh/docs/apisix/plugins/body-transformer/) | APISIX 3.17 default | yes | Partial | yes | - request/response template substitution for `json`, `xml`, `yaml`, `encoded`, `args`, `plain`<br>- bounded `multipart` fields and file names<br>- nested values, array indexes and bracket paths<br>- repeated XML element indexes and `_attr.<name>` lookup | - Templates that use arbitrary Lua statements, loops, or general function execution are not supported. |
| Authentication | [`key-auth`](https://apisix.apache.org/zh/docs/apisix/plugins/key-auth/) | APISIX 3.17 default | yes | 100% | yes | - encrypted consumer fields<br>- header/query API key lookup<br>- APISIX-style missing/invalid key errors<br>- consumer attachment | - None. |
| Authentication | [`jwt-auth`](https://apisix.apache.org/zh/docs/apisix/plugins/jwt-auth/) | APISIX 3.17 default | yes | 100% | yes | - `HS*`, `RS*`, `ES*`, `PS*`, `EdDSA` signature verification<br>- header/query/cookie token lookup<br>- `exp` / `nbf` claim verification<br>- `base64_secret` and PEM/PKIX/PKCS1 key parsing | - None. |
| Authentication | [`jwe-decrypt`](https://apisix.apache.org/zh/docs/apisix/plugins/jwe-decrypt/) | APISIX 3.17 default | yes | 100% | yes | - compact direct AES-256-GCM JWE parsing<br>- `Bearer` token extraction<br>- `kid` consumer lookup<br>- required 32-byte plain/base64url consumer secrets<br>- APISIX-compatible selection by `kid` without validating `alg`, `enc`, or encrypted-key declarations | - None. |
| Authentication | [`basic-auth`](https://apisix.apache.org/zh/docs/apisix/plugins/basic-auth/) | APISIX 3.17 default | yes | 100% | yes | - Basic credential extraction<br>- APISIX-compatible removal of ASCII space/tab/CR/LF/form-feed/vertical-tab from decoded credentials (Unicode whitespace is preserved)<br>- consumer attachment<br>- password validation | - None. |
| Authentication | [`authz-keycloak`](https://apisix.apache.org/zh/docs/apisix/plugins/authz-keycloak/) | APISIX 3.17 default | yes | 100% | yes | - explicit `token_endpoint` or discovery-derived endpoint<br>- static `permissions` and lazy path resource lookup<br>- UMA decision requests<br>- `http_method_as_scope` | - None. |
| Authentication | [`authz-casdoor`](https://apisix.apache.org/zh/docs/apisix/plugins/authz-casdoor/) | APISIX 3.17 default | yes | 100% | yes | - OAuth authorize redirect<br>- per-`client_id` session cookie<br>- callback state validation<br>- AES-GCM stateless session state, independent of route generation and process-local maps | - None. |
| Authentication | [`dingtalk-auth`](https://apisix.apache.org/docs/apisix/plugins/dingtalk-auth/) | APISIX 3.17 default | yes | Partial | yes | - no-code redirect to `redirect_uri`<br>- code extraction from configurable header/query names<br>- APISIX-style invalid-code JSON errors<br>- access token POST with TTL caching | - Session cookies are signed but not encrypted, so their storage confidentiality differs from `resty.session`. |
| Authentication | [`feishu-auth`](https://apisix.apache.org/docs/apisix/plugins/feishu-auth/) | APISIX 3.17 default | yes | Partial | yes | - no-code redirect to `redirect_uri`<br>- code extraction from configurable header/query names<br>- OAuth token POST with `auth_redirect_uri`<br>- userinfo GET with Bearer token | - Session cookies are signed but not encrypted, so their storage confidentiality differs from `resty.session`. |
| Authentication | [`saml-auth`](https://apisix.apache.org/docs/apisix/plugins/saml-auth/) | APISIX 3.17 default | yes | 100% | yes | - HTTP-Redirect and HTTP-POST authentication requests<br>- SP-signed SAML requests<br>- ACS `SAMLResponse` parsing, signature and condition validation through `github.com/crewjam/saml`<br>- signed local SAML session cookies | - None. |
| Authentication | [`wolf-rbac`](https://apisix.apache.org/zh/docs/apisix/plugins/wolf-rbac/) | APISIX 3.17 default | yes | 100% | yes | - `V1#appid#wolf_token` parsing<br>- token extraction from query/header/cookie<br>- consumer lookup by `appid`<br>- Wolf `/wolf/rbac/access_check` | - None. |
| Authentication | [`openid-connect`](https://apisix.apache.org/zh/docs/apisix/plugins/openid-connect/) | APISIX 3.17 default | yes | 100% | yes | - Bearer token extraction from `Authorization` and `X-Access-Token`<br>- discovery fallback for `introspection_endpoint`<br>- token introspection<br>- local JWT issuer validation through discovery or `claim_validator.issuer.valid_issuers`<br>- local JWT audience validation against `client_id` or `claim_validator.audience.valid_audiences`<br>- `client_secret_basic` / `client_secret_post` / `private_key_jwt` / `client_secret_jwt` client authentication | - None. |
| Authentication | [`cas-auth`](https://apisix.apache.org/zh/docs/apisix/plugins/cas-auth/) | APISIX 3.17 default | yes | 100% | yes | - CAS login redirect<br>- absolute/relative `cas_callback_uri`<br>- ticket `serviceValidate`<br>- HMAC-signed initiation cookie | - None. |
| Authentication | [`hmac-auth`](https://apisix.apache.org/zh/docs/apisix/plugins/hmac-auth/) | APISIX 3.17 default | yes | 100% | yes | - `hmac-sha1`, `hmac-sha256`, `hmac-sha512` verification<br>- `signed_headers`<br>- `clock_skew`<br>- request body digest validation | - None. |
| Authentication | [`authz-casbin`](https://apisix.apache.org/zh/docs/apisix/plugins/authz-casbin/) | APISIX 3.17 default | yes | 100% | yes | - Casbin `model` / `policy` text config<br>- `model_path` / `policy_path` file config<br>- plugin-metadata `model` / `policy` fallback with reload on update<br>- configured username header | - None. |
| Authentication | [`ldap-auth`](https://apisix.apache.org/zh/docs/apisix/plugins/ldap-auth/) | APISIX 3.17 default | yes | 100% | yes | - HTTP Basic credential extraction<br>- LDAP bind using configured `base_dn`<br>- host/URL `ldap_uri`<br>- `uid` attribute for user DN construction | - None. |
| Authentication | [`opa`](https://apisix.apache.org/zh/docs/apisix/plugins/opa/) | APISIX 3.17 default | yes | 100% | yes | - OPA HTTP decision calls<br>- custom deny status/body/headers<br>- `send_headers_upstream`<br>- `with_consumer` | - None. |
| Authentication | [`forward-auth`](https://apisix.apache.org/zh/docs/apisix/plugins/forward-auth/) | APISIX 3.17 default | yes | 100% | yes | - `GET` / `POST` with body and transport metadata forwarding<br>- `request_headers`<br>- `extra_headers` with variable resolution<br>- APISIX header precedence: generated headers first, `extra_headers` override, and `request_headers` fill only absent values<br>- `upstream_headers` and `client_headers` | - None. |
| Authentication | [`multi-auth`](https://apisix.apache.org/zh/docs/apisix/plugins/multi-auth/) | APISIX 3.17 default | yes | 100% | yes | - ordered fallback across every 3.17 `type = auth` plugin: `basic-auth`, `key-auth`, `jwt-auth`, `hmac-auth`, `ldap-auth`, `jwe-decrypt`, `wolf-rbac`<br>- every plugin in each `auth_plugins` array object is initialized and executed<br>- request passes when any configured auth plugin succeeds<br>- generic final 401 `Authorization Failed` denial<br>- per-plugin schema validation | - None. |
| Security | [`cors`](https://apisix.apache.org/zh/docs/apisix/plugins/cors/) | APISIX 3.17 default | yes | 100% | yes | - `allow_origins` with defaults<br>- default wildcard omits the plugin-added `Vary: Origin` while preserving existing/downstream `Vary` values<br>- `allow_origins = "**"` request-origin echo<br>- `allow_origins_by_regex`<br>- `allow_origins_by_metadata` | - None. |
| Security | [`acl`](https://apisix.apache.org/zh/docs/apisix/plugins/acl/) | APISIX 3.17 default | yes | Partial | yes | - authenticated consumer `labels`<br>- string, JSON/segmented-text, numeric, boolean, and array label values<br>- `$external_user` label extraction with bounded dotted fields<br>- `$..field[.suffix]` recursive lookup | - Configured JSONPath expressions using bracket or array syntax outside the enumerated Go forms are rejected. |
| Security | [`uri-blocker`](https://apisix.apache.org/zh/docs/apisix/plugins/uri-blocker/) | APISIX 3.17 default | yes | Partial | yes | - `block_rules`<br>- safe invalid-regex rejection<br>- normalized-path matching with original query arguments<br>- concatenated-rule diagnostics | - Configured PCRE-only rules such as lookarounds or backreferences cannot be represented by Go RE2. |
| Security | [`ip-restriction`](https://apisix.apache.org/zh/docs/apisix/plugins/ip-restriction/) | APISIX 3.17 default | yes | 100% | yes | - `whitelist` / `blacklist`<br>- CIDR/IP matching<br>- custom messages<br>- `response_code` 403/404 selection | - None. |
| Security | [`ua-restriction`](https://apisix.apache.org/zh/docs/apisix/plugins/ua-restriction/) | APISIX 3.17 default | yes | Partial | yes | - mutually exclusive `allowlist` or `denylist` schema branches<br>- allow-before-deny matching<br>- `bypass_missing`<br>- trimmed User-Agent matching | - Configured PCRE-only allowlist or denylist rules such as lookarounds or backreferences cannot be represented by Go RE2. |
| Security | [`referer-restriction`](https://apisix.apache.org/zh/docs/apisix/plugins/referer-restriction/) | APISIX 3.17 default | yes | 100% | yes | - `whitelist` / `blacklist`<br>- `bypass_missing` including malformed bare-host Referer values<br>- custom rejection messages<br>- APISIX-style JSON rejection bodies | - None. |
| Security | [`consumer-restriction`](https://apisix.apache.org/zh/docs/apisix/plugins/consumer-restriction/) | APISIX 3.17 default | yes | 100% | yes | - `consumer_name`, `service_id`, `route_id`, `consumer_group_id`<br>- blacklist and whitelist matching<br>- `allowed_by_methods` with the official 10-method enum<br>- custom rejection status/message | - None. |
| Security | [`csrf`](https://apisix.apache.org/zh/docs/apisix/plugins/csrf/) | APISIX 3.17 default | yes | 100% | yes | - token cookie/header presence and equality checks<br>- safe-method bypass<br>- expiry and HMAC-SHA256 signature validation<br>- `expires = 0` no-expiry validation | - None. |
| Security | [`public-api`](https://apisix.apache.org/zh/docs/apisix/plugins/public-api/) | APISIX 3.17 default | yes | 100% | yes | - method+URI registry with `Register` / `Lookup`<br>- optional `uri` override<br>- `node-status` at `/apisix/status`<br>- `server-info` at `/v1/server_info` | - None. |
| Security | [`GM`](https://apisix.apache.org/zh/docs/apisix/plugins/GM/) | README-listed extra/native | yes | N/A | yes | - No Go HTTP-plugin behavior; the registered marker is not installed as production SSL enforcement. | - N/A: GM/Tongsuo certificate enforcement belongs to the native TLS listener rather than an HTTP plugin. |
| Security | [`chaitin-waf`](https://apisix.apache.org/zh/docs/apisix/plugins/chaitin-waf/) | APISIX 3.17 default | yes | 100% | yes | - off/monitor/block modes and expressions<br>- metadata/config node selection and quarantine<br>- bounded inspection copy with full upstream-body replay<br>- official decision headers and block response | - None. |
| Security | [`data-mask`](https://apisix.apache.org/zh/docs/apisix/plugins/data-mask/) | APISIX 3.17 default | yes | Partial | yes | - detached log-snapshot-only query/header/urlencoded/JSON body masking; upstream request URI/query order, headers, and body bytes remain unchanged<br>- bounded `max_req_post_args` parsing with fail-closed logging when the form exceeds the configured argument count<br>- APISIX conditional rule-schema validation<br>- bounded JSONPath body masking for dot paths, root-array selectors, quoted bracket fields, recursive descent, `[*]`, and numeric indexes | - Configured JSONPath unions, slices, and unbracketed expressions are rejected by the bounded parser. |
| Security | [`oas-validator`](https://apisix.apache.org/docs/apisix/plugins/oas-validator/) | APISIX 3.17 default | yes | 100% | yes | - inline/remote OpenAPI specs with secret-backed headers<br>- bounded external-reference graph with cycle rejection<br>- SSRF-safe fetch, redirects, address allowlist, and origin-scoped headers<br>- kin-openapi request validation with refresh and last-good retention | - None. |
| Traffic | [`limit-req`](https://apisix.apache.org/zh/docs/apisix/plugins/limit-req/) | APISIX 3.17 default | yes | 100% | yes | - local, Redis, and Redis Cluster token buckets<br>- bounded ref-counted local stores and shared Redis clients<br>- route-scoped variable/header keys<br>- rejection, `nodelay`, and degradation controls | - None. |
| Traffic | [`limit-conn`](https://apisix.apache.org/zh/docs/apisix/plugins/limit-conn/) | APISIX 3.17 default | yes | 100% | yes | - local, Redis, and Redis Cluster connection limits<br>- atomic admission and request-finalizer release<br>- route/rule variable keys and adaptive delay<br>- rejection and degradation controls | - None. |
| Traffic | [`limit-count`](https://apisix.apache.org/zh/docs/apisix/plugins/limit-count/) | APISIX 3.17 default | yes | 100% | yes | - local, Redis, and Redis Cluster fixed windows<br>- bounded ref-counted groups and shared backends<br>- dynamic rule/variable quotas<br>- quota headers, rejection, and degradation controls | - None. |
| Traffic | [`graphql-limit-count`](https://apisix.apache.org/docs/apisix/plugins/graphql-limit-count/) | APISIX 3.17 default | yes | 100% | yes | - bounded JSON/GraphQL parsing and depth cost<br>- fragment-cycle and undefined-fragment rejection<br>- local/Redis/Cluster quotas with shared backends<br>- bounded ref-counted groups and config mismatch rejection | - None. |
| Traffic | [`proxy-cache`](https://apisix.apache.org/zh/docs/apisix/plugins/proxy-cache/) | APISIX 3.17 default | yes | 100% | yes | - memory/disk zones with versioned envelopes<br>- shared reload, `Vary`, `PURGE`, and expiry lifecycle<br>- cache-control/TTL/cookie/consumer policies<br>- GET reuse and HEAD-miss store safety | - None. |
| Traffic | [`graphql-proxy-cache`](https://apisix.apache.org/docs/apisix/plugins/graphql-proxy-cache/) | APISIX 3.17 default | yes | 100% | yes | - GET/POST validation bounded by `graphql.max_size`<br>- grammar parsing for operations, variables, types, args, directives, fragments, aliases, values, strings, numbers<br>- JSON and `application/graphql` bodies<br>- mutation bypass with `Apisix-Cache-Status: BYPASS` | - None. |
| Traffic | [`request-validation`](https://apisix.apache.org/zh/docs/apisix/plugins/request-validation/) | APISIX 3.17 default | yes | Partial | yes | - JSON/form body and header schemas<br>- missing/blank body and decode rejection<br>- `UseNumber` JSON normalization with exact replay<br>- config-time nested-schema validation | - Secret references used as `header_schema` or `body_schema` are not materialized before validation. |
| Traffic | [`proxy-mirror`](https://apisix.apache.org/zh/docs/apisix/plugins/proxy-mirror/) | APISIX 3.17 default | yes | Partial | yes | - HTTP mirror `host`, `path`, `path_concat_mode`, `sample_ratio`<br>- APISIX-style `host` / `path` schema validation<br>- bounded request-body capture with exact accepted-body replay and pre-upstream 413 rejection<br>- bounded unary gRPC request mirroring over HTTP/2 | - Mirror hosts bypass the APISIX DNS resolver, and long-lived streaming mirror timing is not equivalent. |
| Traffic | [`kafka-proxy`](https://apisix.apache.org/docs/apisix/plugins/kafka-proxy/) | APISIX 3.17 default | yes | 100% | yes | - strict `sasl.password` secret resolution<br>- request-context propagation to the stream consumer<br>- APISIX PubSub protobuf WebSocket owner (`cmd_kafka_list_offset`, `cmd_kafka_fetch`) with sequence preservation<br>- text frames are ignored; malformed binary commands receive sequence-0 `wrong command` and keep the session open<br>- offset/timestamp/key/value conversion through a bounded `kafka-go` consumer | - None. |
| Traffic | [`dubbo-proxy`](https://apisix.apache.org/docs/apisix/plugins/dubbo-proxy/) | Registered non-default | yes | 100% | no | - required `service_name` / `service_version`<br>- URI-derived method fallback<br>- Hessian2 `Map<String,Object>` HTTP-context request encoding<br>- route-upstream TCP terminal integration with retries | - None. |
| Traffic | [`http-dubbo`](https://apisix.apache.org/docs/apisix/plugins/http-dubbo/) | APISIX 3.17 default | yes | 100% | yes | - cluster-selected TCP upstreams with retries/probes<br>- bounded connect/send/read timeouts, including fractional millisecond configuration<br>- Dubbo 2.x fastjson request framing<br>- response frame validation and HTTP mapping | - None. |
| Traffic | [`api-breaker`](https://apisix.apache.org/zh/docs/apisix/plugins/api-breaker/) | APISIX 3.17 default | yes | Partial | yes | - `break_response_code`, `break_response_body`<br>- `break_response_headers` with bounded variable resolution<br>- `max_breaker_sec` with exponential backoff<br>- `unhealthy.http_statuses` / `unhealthy.failures` | - Breaker counters are route-local instead of shared by host and URI across routes, which can change trip and recovery behavior. |
| Traffic | [`traffic-split`](https://apisix.apache.org/zh/docs/apisix/plugins/traffic-split/) | APISIX 3.17 default | yes | Partial | yes | - compiled match expressions and bounded form reads<br>- resolved inline/upstream-ID weighted targets with node-priority fallback<br>- APISIX-compatible ketama/variable hashing<br>- cluster-owned retries, progress timeouts, and active health probes | - `match.vars` expressions that require unsupported NGINX variables or PCRE-only semantics can differ. |
| Traffic | [`traffic-label`](https://apisix.apache.org/zh/docs/apisix/plugins/traffic-label/) | APISIX 3.17 default | yes | Partial | yes | - schema-validated first-match rules<br>- match-all rules<br>- string/numeric `set_headers` with variable resolution<br>- weighted actions via per-weight round-robin sequences | - Multi-action selection uses a sequential cursor instead of APISIX cached random round-robin, which changes selection order. |
| Traffic | [`request-id`](https://apisix.apache.org/zh/docs/apisix/plugins/request-id/) | APISIX 3.17 default | yes | 100% | yes | - custom header names via `header_name`<br>- response header opt-out via `include_in_response`<br>- incoming request ID preservation<br>- `uuid`, `uuidv7`, `nanoid`, `ksuid`, `range_id` algorithms | - None. |
| Traffic | [`proxy-control`](https://apisix.apache.org/zh/docs/apisix/plugins/proxy-control/) | APISIX 3.17 default | yes | 100% | yes | - route/global `request_buffering` flag propagation<br>- eager Go proxy request-body buffering before upstream forwarding | - None. |
| Traffic | [`proxy-buffering`](https://apisix.apache.org/zh/docs/apisix/plugins/proxy-buffering/) | APISIX 3.17 default | yes | 100% | yes | - route/global `disable_proxy_buffering` flag propagation<br>- immediate response flushing via the flushing response writer<br>- streaming handler selection when buffering is disabled | - None. |
| Traffic | [`client-control`](https://apisix.apache.org/zh/docs/apisix/plugins/client-control/) | APISIX 3.17 default | yes | 100% | yes | - route-level `max_body_size` enforcement<br>- 413 for declared/chunked overflow<br>- 500 for non-size body-read failures<br>- exact accepted-body replay | - None. |
| Traffic | [`workflow`](https://apisix.apache.org/zh/docs/apisix/plugins/workflow/) | APISIX 3.17 default | yes | Partial | yes | - official action-array config shape (`["name", config]`)<br>- first matching `case` with config-time expression compile<br>- bounded APISIX expression operators and request variables<br>- validated `return` actions with configured status code | - `limit-count` actions with `group` are rejected, and `case` expressions using unsupported NGINX variables or PCRE-only semantics can differ. |
| Observability / Tracers | [`zipkin`](https://apisix.apache.org/zh/docs/apisix/plugins/zipkin/) | APISIX 3.17 default | yes | Partial | no | - B3 single-header and multi-header extraction/injection<br>- parent/child span identity with random IDs<br>- probabilistic `sample_ratio`<br>- `endpoint`, `service_name`, `server_addr` | - `span_version = 1` is rejected, and APISIX per-phase span timing fields are not emitted. |
| Observability / Tracers | [`skywalking`](https://apisix.apache.org/zh/docs/apisix/plugins/skywalking/) | README-listed extra/native | yes | 100% | yes | - probabilistic `sample_ratio`<br>- `plugin_attr.skywalking` service/instance/endpoint/report_interval defaults<br>- `sw8` parse/injection with 8-field base64url format<br>- the last rewrite binding owns trace state, so route configuration overrides global configuration<br>- unsampled final bindings preserve a client `sw8` and remove only plugin-generated propagation<br>- random trace/segment IDs and cross-process segment refs | - None. |
| Observability / Tracers | [`opentelemetry`](https://apisix.apache.org/zh/docs/apisix/plugins/opentelemetry/) | README-listed extra/native | yes | Partial | yes | - official plugin name and priority<br>- sampler names `always_on`, `always_off`, `trace_id_ratio`, `parent_base` with defaults<br>- configurable middleware server name<br>- `additional_attributes` and `additional_header_prefix_attributes` | - `set_ngx_var` is schema-accepted but has no Go request-variable effect. |
| Observability / Metrics | [`prometheus`](https://apisix.apache.org/zh/docs/apisix/plugins/prometheus/) | APISIX 3.17 default | yes | 100% | no | - collection disabled when the plugin is absent from the global allowlist<br>- request metrics owned by the effective route/service/plugin-config/global binding with `prefer_route` behavior<br>- dedicated export listener or configured `public-api` URI<br>- official connection, request, node, etcd-index, upstream-health, stream, HTTP, and LLM family names/labels<br>- bounded series with idle expiration | - None. |
| Observability / Metrics | [`node-status`](https://apisix.apache.org/zh/docs/apisix/plugins/node-status/) | README-listed extra/native | yes | 100% | yes | - `/apisix/status` endpoint registration<br>- configured IDs and persisted generated `conf/apisix.uid` IDs<br>- process-wide active/accepted/handled/total HTTP request counters<br>- JSON `id` + `status` response shape | - None. |
| Observability / Metrics | [`datadog`](https://apisix.apache.org/zh/docs/apisix/plugins/datadog/) | APISIX 3.17 default | yes | 100% | yes | - DogStatsD UDP metrics<br>- metadata `host`, `port`, `namespace`, `constant_tags`<br>- route `constant_tags` and `prefer_name`<br>- consumer/balancer/status/scheme tags | - None. |
| Observability / Loggers | [`http-logger`](https://apisix.apache.org/zh/docs/apisix/plugins/http-logger/) | APISIX 3.17 default | yes | 100% | yes | - `uri`, encrypted `auth_header`, `timeout`, `ssl_verify`, HTTP/S POST delivery<br>- route/metadata nested `log_format` with depth-five cap<br>- default route/request/response/consumer fields<br>- `concat_method` JSON/new-line batches | - None. |
| Observability / Loggers | [`skywalking-logger`](https://apisix.apache.org/zh/docs/apisix/plugins/skywalking-logger/) | APISIX 3.17 default | yes | 100% | yes | - `endpoint_addr`, `service_name`, `service_instance_name`, `timeout`, `log_format`<br>- `/v3/logs` delivery with basic `sw8` trace correlation<br>- `include_req_body` / `include_req_body_expr`<br>- `include_resp_body` / `include_resp_body_expr` with capped capture | - None. |
| Observability / Loggers | [`tcp-logger`](https://apisix.apache.org/zh/docs/apisix/plugins/tcp-logger/) | APISIX 3.17 default | yes | 100% | yes | - `host`, `port`, `timeout`, `log_format`<br>- `tls`, `tls_options` as TLS server name / SNI<br>- TLS 1.2 minimum and `ssl_verify` with secure default plus explicit compatibility opt-out<br>- default access-log credential-header redaction while explicit custom formats remain available | - None. |
| Observability / Loggers | [`kafka-logger`](https://apisix.apache.org/zh/docs/apisix/plugins/kafka-logger/) | APISIX 3.17 default | yes | 100% | yes | - `brokers`, deprecated `broker_list`, `kafka_topic`, `key`, `producer_type`, `required_acks`, `timeout`<br>- broker `sasl_config` for `PLAIN` / `SCRAM-SHA-256` / `SCRAM-SHA-512`<br>- encrypted broker SASL passwords resolved strictly before writer creation; rotated keys supported and invalid ciphertext fails initialization<br>- producer batch defaults and `meta_format = origin` | - None. |
| Observability / Loggers | [`rocketmq-logger`](https://apisix.apache.org/zh/docs/apisix/plugins/rocketmq-logger/) | APISIX 3.17 default | yes | Partial | yes | - `nameserver_list`, `topic`, `key`, `tag`, `timeout`<br>- `access_key`, encrypted `secret_key`<br>- `meta_format = origin`, `log_format`<br>- `include_req_body` / `include_req_body_expr` | - `use_tls = true` is rejected because the pinned Go client exposes no TLS transport hook. |
| Observability / Loggers | [`udp-logger`](https://apisix.apache.org/zh/docs/apisix/plugins/udp-logger/) | APISIX 3.17 default | yes | 100% | yes | - `host`, `port`, `timeout`, `log_format`, UDP delivery<br>- `include_req_body` / `include_req_body_expr`<br>- `include_resp_body` / `include_resp_body_expr` with capped capture<br>- batch processor fields | - None. |
| Observability / Loggers | [`clickhouse-logger`](https://apisix.apache.org/zh/docs/apisix/plugins/clickhouse-logger/) | APISIX 3.17 default | yes | 100% | yes | - `endpoint_addr`, random `endpoint_addrs` selection<br>- `user`, encrypted `password`, `database`, `logtable`, `timeout`, `ssl_verify`, `log_format`<br>- `include_req_body` / `include_req_body_expr`<br>- `include_resp_body` / `include_resp_body_expr` with capped capture | - None. |
| Observability / Loggers | [`syslog`](https://apisix.apache.org/zh/docs/apisix/plugins/syslog/) | APISIX 3.17 default | yes | 100% | yes | - exact raw RFC5424 SYSLOG/INFO frames (`<46>1`, millisecond UTC timestamp, host, `apisix`, PID, JSON, newline)<br>- TCP, UDP, and TLS delivery with ordered frame concatenation<br>- TLS 1.2 minimum and `ssl_verify` with secure default plus explicit compatibility opt-out<br>- default access-log credential-header redaction while explicit custom formats remain available | - None. |
| Observability / Loggers | [`log-rotate`](https://apisix.apache.org/zh/docs/apisix/plugins/log-rotate/) | README-listed extra/native | yes | 100% | yes | - `plugin_attr.log-rotate` defaults for `interval`, `max_kept`, `max_size`, `timeout`, `enable_compression`<br>- APISIX timestamped `__access.log` / `__error.log` naming<br>- max-size rotation and interval checks on request<br>- current-file recreation and Go-side file-logger path reopen after rotation | - None. |
| Observability / Loggers | [`error-log-logger`](https://apisix.apache.org/zh/docs/apisix/plugins/error-log-logger/) | README-listed extra/native | yes | 100% | yes | - official metadata-shaped config for `tcp`, `skywalking`, `clickhouse`, `kafka`<br>- legacy `host` / `port` TCP config<br>- level filtering with observer replacement<br>- TCP/TLS delivery, SkyWalking `/v3/logs` with `$hostname` resolution, ClickHouse JSONEachRow, Kafka topic/key publishing | - None. |
| Observability / Loggers | [`sls-logger`](https://apisix.apache.org/zh/docs/apisix/plugins/sls-logger/) | APISIX 3.17 default | yes | 100% | yes | - RFC5424 SLS log messages over TLS TCP<br>- `host`, `port`, `project`, `logstore`, `access_key_id`, encrypted `access_key_secret`, `timeout`, `log_format`; the required secret is validated for official-schema compatibility, then cleared and never serialized<br>- `include_req_body` / `include_req_body_expr`<br>- `include_resp_body` / `include_resp_body_expr` with capped capture | - None. |
| Observability / Loggers | [`google-cloud-logging`](https://apisix.apache.org/zh/docs/apisix/plugins/google-cloud-logging/) | APISIX 3.17 default | yes | 100% | yes | - service-account `auth_config` / `auth_file` with encrypted private key<br>- JWT bearer token exchange with cached, skew-aware refresh<br>- `entries_uri`, `resource`, `log_id`, `ssl_verify`, `log_format`<br>- default Cloud Logging `httpRequest` entry expansion | - None. |
| Observability / Loggers | [`splunk-hec-logging`](https://apisix.apache.org/zh/docs/apisix/plugins/splunk-hec-logging/) | APISIX 3.17 default | yes | 100% | yes | - `endpoint.uri`, encrypted `endpoint.token`, `endpoint.channel`, `endpoint.timeout`, `endpoint.keepalive_timeout`<br>- `ssl_verify`, `log_format`, `log_format_extra`<br>- batch processor fields<br>- concatenated JSON event-object batch payloads and `max_pending_entries` | - None. |
| Observability / Loggers | [`file-logger`](https://apisix.apache.org/zh/docs/apisix/plugins/file-logger/) | APISIX 3.17 default | yes | 100% | yes | - `path` from route or metadata<br>- `log_format` / `log_format_extra` precedence with depth-five cap<br>- default credential-header redaction while explicit custom formats remain available<br>- newly created log files use owner-only `0600` mode without chmodding existing files<br>- bounded async entry queue plus shared per-path zap byte buffer with one-second flush and rotation-safe reopen<br>- pending, bounded-overload, and lifecycle batch metrics | - None. |
| Observability / Loggers | [`loggly`](https://apisix.apache.org/zh/docs/apisix/plugins/loggly/) | APISIX 3.17 default | yes | 100% | yes | - RFC5424 Loggly syslog messages over UDP and HTTP/S bulk endpoint delivery<br>- encrypted `customer_token` with standalone encryption at rest and plugin-boundary decryption<br>- `severity`, response-status `severity_map`, `tags`, `host`, `port`, `protocol`, `timeout`, `ssl_verify`<br>- metadata/route `log_format` precedence with route identity | - None. |
| Observability / Loggers | [`elasticsearch-logger`](https://apisix.apache.org/zh/docs/apisix/plugins/elasticsearch-logger/) | APISIX 3.17 default | yes | 100% | yes | - `endpoint_addr`, random `endpoint_addrs` selection<br>- `field.index` with time and APISIX variable expansion<br>- major-version discovery with `_type = _doc` only for versions 5/6, ignored `field.type` on modern versions<br>- `auth`, `headers`, `timeout`, `ssl_verify`, `log_format` | - None. |
| Observability / Loggers | [`tencent-cloud-cls`](https://apisix.apache.org/zh/docs/apisix/plugins/tencent-cloud-cls/) | APISIX 3.17 default | yes | 100% | yes | - `cls_host`, `cls_topic`, `scheme`, `ssl_verify`<br>- encrypted `secret_id` / `secret_key`<br>- `sample_ratio`, `global_tag`, `log_format`<br>- `include_req_body` / `include_req_body_expr`, `include_resp_body` / `include_resp_body_expr` with capped capture | - None. |
| Observability / Loggers | [`loki-logger`](https://apisix.apache.org/zh/docs/apisix/plugins/loki-logger/) | APISIX 3.17 default | yes | 100% | yes | - random `endpoint_addrs` selection, `endpoint_uri`, `tenant_id`<br>- custom headers and `log_labels` with variable resolution<br>- `ssl_verify`, `timeout`, keepalive defaults<br>- route/metadata `log_format` / `log_format_extra` | - None. |
| Observability / Loggers | [`lago`](https://apisix.apache.org/docs/apisix/plugins/lago/) | APISIX 3.17 default | yes | 100% | yes | - random `endpoint_addrs` selection with Bearer token delivery to `endpoint_uri`<br>- request/response variable templates for transaction ID, subscription ID, event properties<br>- request-start event timestamps and dynamic `${arg_*}`, `${cookie_*}`, `${http_*}`, `${sent_http_*}`, `${upstream_http_*}` variables<br>- `include_req_body` / `include_resp_body` with capped capture through `${request_body}` / `${response_body}` templates | - None. |
| AI | [`ai`](https://github.com/apache/apisix/blob/release/3.17/apisix/plugins/ai.lua) | APISIX 3.17 default | yes | N/A | no | - No route-level configuration surface; the registered factory fails closed if someone tries to install the Lua runtime coordinator as a Go handler. | - N/A: APISIX uses this empty-schema module as a Lua router and balancer coordinator; Go owns those responsibilities in its native pipeline. |
| AI | [`ai-aliyun-content-moderation`](https://apisix.apache.org/docs/apisix/plugins/ai-aliyun-content-moderation/) | APISIX 3.17 default | yes | 100% | no | - request and response moderation with protocol-aware content extraction<br>- `stream_check_mode` realtime/final_packet with cache size and interval<br>- Aliyun TextModerationPlus HMAC-SHA1 signing with session nonce reuse<br>- `risk_level_bar`, `deny_code`, `deny_message`, `check_request`, `check_response` | - None. |
| AI | [`ai-aws-content-moderation`](https://apisix.apache.org/docs/apisix/plugins/ai-aws-content-moderation/) | APISIX 3.17 default | yes | 100% | yes | - AWS Comprehend request checks<br>- SigV4 with static and session credentials<br>- encrypted secrets<br>- endpoint override and TLS controls | - None. |
| AI | [`ai-rag`](https://apisix.apache.org/docs/apisix/plugins/ai-rag/) | APISIX 3.17 default | yes | 100% | yes | - `embeddings_provider.azure_openai` and `vector_search_provider.azure_ai_search` schema<br>- body `ai_rag` field validation<br>- embeddings request and vector search with `api-key` auth<br>- search result injected as a user message via protocol append | - None. |
| AI | [`ai-prompt-decorator`](https://apisix.apache.org/docs/apisix/plugins/ai-prompt-decorator/) | APISIX 3.17 default | yes | 100% | yes | - `prepend` / `append` prompt arrays with role/content schema<br>- protocol detection and message injection<br>- JSON body rewrite and replace | - None. |
| AI | [`ai-prompt-guard`](https://apisix.apache.org/docs/apisix/plugins/ai-prompt-guard/) | APISIX 3.17 default | yes | Partial | yes | - allow/deny regex policy evaluation with compiled patterns<br>- shared extraction across all six AI protocols<br>- nested content blocks via Responses API parts<br>- `match_all_roles`, `match_all_conversation_history`, `fail_mode` | - Configured PCRE-only guard patterns cannot be represented by Go RE2. |
| AI | [`ai-prompt-template`](https://apisix.apache.org/docs/apisix/plugins/ai-prompt-template/) | APISIX 3.17 default | yes | 100% | no | - named `templates` array with model/messages<br>- `template_name` body lookup with official missing/unknown-template errors<br>- `{{ expr }}` rendering with nested dotted and bounded indexed lookup<br>- request body replacement and continuation upstream | - None. |
| AI | [`ai-request-rewrite`](https://apisix.apache.org/docs/apisix/plugins/ai-request-rewrite/) | APISIX 3.17 default | yes | 100% | yes | - `prompt` / `provider` / `auth` schema with a ten-provider enum<br>- LLM call and body replacement with extracted text<br>- AWS SigV4 and GCP token auth paths<br>- `$llm_request_body` / `$ai_request_body_changed` variable registration | - None. |
| AI | [`ai-rate-limiting`](https://apisix.apache.org/docs/apisix/plugins/ai-rate-limiting/) | APISIX 3.17 default | yes | 100% | yes | - global, per-instance, rule, and expression quotas<br>- local, Redis, and Sentinel atomic fixed windows<br>- four token strategies with two-phase charging<br>- non-buffering stream charging, fallback, and quota headers | - None. |
| AI | [`ai-proxy`](https://apisix.apache.org/docs/apisix/plugins/ai-proxy/) | APISIX 3.17 default | yes | 100% | yes | - six-protocol detection including passthrough<br>- Anthropic-to-OpenAI request/response/SSE conversion with tools<br>- OpenAI Responses and Embeddings<br>- Vertex embeddings conversion both directions<br>- provider 429/5xx responses return status only without provider body/headers; transport timeout maps to 504 and other transport errors to 500 without raw error text | - None. |
| AI | [`ai-proxy-multi`](https://apisix.apache.org/docs/apisix/plugins/ai-proxy-multi/) | APISIX 3.17 default | yes | Partial | no | - weighted priorities with descending sort<br>- round-robin and APISIX 3.17-compatible 160-point ketama chash across vars/header/cookie/consumer/variable-combination keys with remote-address fallback<br>- HTTP/rate fallback and retries with `max_retries` / `retry_on_failure_within_ms`<br>- active health schema with threshold/interval behavior | - Domains are not expanded into per-IP nodes, so DNS snapshots and per-address health/failover behavior can differ. |
| AI | [`mcp-bridge`](https://github.com/apache/apisix/blob/release/3.17/apisix/plugins/mcp-bridge.lua) | APISIX 3.17 default | yes | 100% | no | - stdio subprocess launch with `command` / `args`<br>- `GET {base_uri}/sse` with advertised message endpoints<br>- 30-second JSON-RPC ping keepalives<br>- bounded `POST {base_uri}/message` forwarding with 413 rejection | - None. |
| Stream | [`mqtt-proxy`](https://apisix.apache.org/docs/apisix/plugins/mqtt-proxy/) | Registered non-default | yes | 100% | no | - default `protocol_name = "MQTT"`<br>- MQTT 5 CONNECT decoded with Eclipse Paho packets; MQTT 3.1/3.1.1 with the local bounded parser<br>- flags validation and UTF-8 validation with zero-byte rejection<br>- client-ID extraction with peer-address fallback | - None. |
| Development | [`example-plugin`](https://github.com/apache/apisix/blob/release/3.17/apisix/plugins/example-plugin.lua) | APISIX 3.17 default | yes | 100% | yes | - metadata schema<br>- pass-through middleware<br>- route upstream override through the traffic-split path when `ip` is configured<br>- control API `GET /v1/plugin/example-plugin/hello` with text or JSON response | - None. |

## Cross-plugin remaining work

These items are cross-cutting follow-ups, not automatic deductions from an individual plugin's compatibility:

1. **General stream-plugin context:** define a stream context and lifecycle owner for every supported stream plugin, then add end-to-end fixtures. The current stream design is documented in [`design.md`](design.md).
2. **Upstream health-status API:** active/passive probe scheduling is cluster-owned; define persistence and `/v1/healthcheck` semantics before exposing state.
3. **Kafka external-broker smoke coverage:** add only when an external integration environment and credential-safe CI contract are available.
4. **Concrete expression, regex, or schema mismatches:** reproduce the APISIX-vs-Go mismatch first, then add the smallest regression and fix.

## Scope boundary

Compatibility is judged from APISIX 3.17 configuration and externally observable behavior. Different Go-native implementations of caching, concurrency, buffering, transport pooling, timers, or lifecycle phases do not reduce compatibility by themselves. Arbitrary Lua/`ngx_lua`, LuaJIT inspection, NGINX-only metrics and TLS stapling, and the external plugin-runner protocol are `N/A` unless a separate Go-facing contract is explicitly adopted.
