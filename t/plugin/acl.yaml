cases:
  - name: allow-consumers-by-org
    runtime: &consumer_runtime
      plugins: [acl, basic-auth]
    config:
      routes:
        - id: acl-allow-org
          uri: /hello
          plugins:
            basic-auth: {}
            acl:
              allow_labels:
                org: [apache]
          upstream: &fixture_upstream
            type: roundrobin
            nodes:
              "{{FIXTURE.primary.ADDR}}": 1
      consumers: &consumers
        - username: jack
          labels:
            org: apache
            project: gateway,apisix,web-server
          plugins:
            basic-auth:
              username: jack
              password: "123456"
        - username: rose
          labels:
            org: '["opensource","apache"]'
            project: '["tomcat","web-server","http,server"]'
          plugins:
            basic-auth:
              username: rose
              password: "123456"
    fixtures:
      - &hello_fixture
        name: primary
        kind: http
        respond:
          - status: 200
            body: hello world
    steps:
      - name: unauthorized
        input:
          path: /hello
        output: &missing_basic_auth
          status: 401
          body:
            matches: 'Missing authorization in request'
      - name: jack-allowed
        input:
          path: /hello
          headers:
            Authorization: Basic amFjazoxMjM0NTY=
        output: &hello_output
          status: 200
          body:
            equals: hello world
      - name: rose-allowed
        input:
          path: /hello
          headers:
            Authorization: Basic cm9zZToxMjM0NTY=
        output: *hello_output

  - name: allow-consumer-by-project
    runtime: *consumer_runtime
    config:
      routes:
        - id: acl-allow-project
          uri: /hello
          plugins:
            basic-auth: {}
            acl:
              allow_labels:
                project: [apisix]
          upstream: *fixture_upstream
      consumers: *consumers
    fixtures: [*hello_fixture]
    steps:
      - name: jack-allowed
        input:
          path: /hello
          headers:
            Authorization: Basic amFjazoxMjM0NTY=
        output: *hello_output
      - name: rose-forbidden
        input:
          path: /hello
          headers:
            Authorization: Basic cm9zZToxMjM0NTY=
        output: &acl_forbidden
          status: 403
          body:
            matches: 'The consumer is forbidden\.'

  - name: deny-consumer-by-project-with-custom-message
    runtime: *consumer_runtime
    config:
      routes:
        - id: acl-deny-project
          uri: /hello
          plugins:
            basic-auth: {}
            acl:
              deny_labels:
                project: [apisix]
              rejected_msg: request is forbidden
          upstream: *fixture_upstream
      consumers: *consumers
    fixtures: [*hello_fixture]
    steps:
      - name: jack-forbidden
        input:
          path: /hello
          headers:
            Authorization: Basic amFjazoxMjM0NTY=
        output:
          status: 403
          body:
            matches: 'request is forbidden'
      - name: rose-allowed
        input:
          path: /hello
          headers:
            Authorization: Basic cm9zZToxMjM0NTY=
        output: *hello_output

  - name: deny-consumers-by-multiple-project-values
    runtime: *consumer_runtime
    config:
      routes:
        - id: acl-deny-multiple-projects
          uri: /hello
          plugins:
            basic-auth: {}
            acl:
              deny_labels:
                project: [apisix, tomcat]
          upstream: *fixture_upstream
      consumers: *consumers
    fixtures: [*hello_fixture]
    steps:
      - name: jack-forbidden
        input:
          path: /hello
          headers:
            Authorization: Basic amFjazoxMjM0NTY=
        output: *acl_forbidden
      - name: rose-forbidden
        input:
          path: /hello
          headers:
            Authorization: Basic cm9zZToxMjM0NTY=
        output: *acl_forbidden

  - name: preserve-comma-in-label-value
    runtime: *consumer_runtime
    config:
      routes:
        - id: acl-comma-label
          uri: /hello
          plugins:
            basic-auth: {}
            acl:
              allow_labels:
                project: ["http,server"]
          upstream: *fixture_upstream
      consumers: *consumers
    fixtures: [*hello_fixture]
    steps:
      - name: jack-forbidden
        input:
          path: /hello
          headers:
            Authorization: Basic amFjazoxMjM0NTY=
        output: *acl_forbidden
      - name: rose-allowed
        input:
          path: /hello
          headers:
            Authorization: Basic cm9zZToxMjM0NTY=
        output: *hello_output

  - name: external-user-query-values
    runtime: &external_runtime
      plugins: [acl, serverless-pre-function]
    config:
      routes:
        - id: acl-external-query
          uri: /hello
          plugins:
            serverless-pre-function:
              phase: access
              functions:
                - >-
                  return function(conf, ctx)
                    local teams = {}
                    for team in string.gmatch(ctx.var.request_uri, "team=([^&]+)") do
                      table.insert(teams, team)
                    end
                    ctx.external_user = { team = teams }
                  end
            acl:
              external_user_label_field: team
              allow_labels:
                team: [cloud, infra, devops, qa]
          upstream: *fixture_upstream
    fixtures: [*hello_fixture]
    steps:
      - name: infra-allowed
        input:
          path: /hello?team=infra
        output: *hello_output
      - name: one-matching-team-allowed
        input:
          path: /hello?team=infra&team=fake
        output: *hello_output
      - name: fake-forbidden
        input:
          path: /hello?team=fake
        output: *acl_forbidden

  - name: external-user-table-parser
    runtime: *external_runtime
    config:
      routes:
        - id: acl-external-table
          uri: /hello
          plugins:
            serverless-pre-function:
              phase: access
              functions:
                - 'return function(conf, ctx) ctx.external_user = { orgs = { api7 = { team = {"cloud", "infra"} } } } end'
            acl:
              allow_labels:
                org: [api7, apache]
                team: [cloud, infra]
              external_user_label_field: $.orgs..team
              external_user_label_field_key: team
              external_user_label_field_parser: table
              rejected_code: 403
          upstream: &single_fixture_upstream
            type: roundrobin
            nodes:
              "{{UPSTREAM_ADDR}}": 1
    upstream: &hello_upstream
      respond:
        status: 200
        body: hello world
    input:
      path: /hello
    output: *hello_output

  - name: external-user-segmented-text-parser
    runtime: *external_runtime
    config:
      routes:
        - id: acl-external-segmented
          uri: /hello
          plugins:
            serverless-pre-function:
              phase: access
              functions:
                - 'return function(conf, ctx) ctx.external_user = { orgs = { api7 = { team = "cloud|infra" } } } end'
            acl:
              allow_labels:
                org: [api7, apache]
                team: [cloud, infra]
              external_user_label_field: $.orgs..team
              external_user_label_field_key: team
              external_user_label_field_parser: segmented_text
              external_user_label_field_separator: '\|'
              rejected_code: 403
          upstream: *single_fixture_upstream
    upstream: *hello_upstream
    input:
      path: /hello
    output: *hello_output

  - name: external-user-json-parser
    runtime: *external_runtime
    config:
      routes:
        - id: acl-external-json
          uri: /hello
          plugins:
            serverless-pre-function:
              phase: access
              functions:
                - 'return function(conf, ctx) ctx.external_user = { orgs = { api7 = { team = "[\"cloud\", \"infra\"]" } } } end'
            acl:
              allow_labels:
                org: [api7, apache]
                team: [cloud, infra]
              external_user_label_field: $..team
              external_user_label_field_key: team
              external_user_label_field_parser: json
              rejected_code: 403
          upstream: *single_fixture_upstream
    upstream: *hello_upstream
    input:
      path: /hello
    output: *hello_output

  - name: segmented-text-invalid-separator-does-not-match
    runtime: *external_runtime
    config:
      routes:
        - id: acl-external-separator-literal
          uri: /hello
          plugins:
            serverless-pre-function:
              phase: access
              functions:
                - 'return function(conf, ctx) ctx.external_user = { orgs = { api7 = { team = "cloud|infra" } } } end'
            acl:
              allow_labels:
                org: [api7, apache]
                team: [cloud, infra]
              external_user_label_field: $.orgs..team
              external_user_label_field_key: team
              external_user_label_field_parser: segmented_text
              external_user_label_field_separator: '|'
              rejected_code: 403
          upstream: *single_fixture_upstream
    upstream: *hello_upstream
    input:
      path: /hello
    output: *acl_forbidden

  - name: external-user-multiple-jsonpath-values
    runtime: *external_runtime
    config:
      routes:
        - id: acl-external-multiple
          uri: /hello
          plugins:
            serverless-pre-function:
              phase: access
              functions:
                - 'return function(conf, ctx) ctx.external_user = { orgs = { api7 = { team = "cloud|infra" }, apache = { team = {"devops", "qa"} } } } end'
            acl:
              allow_labels:
                org: [api7, apache]
                team: [cloud, infra]
              external_user_label_field: $.orgs..team
              external_user_label_field_key: team
              external_user_label_field_parser: segmented_text
              external_user_label_field_separator: '\|'
              rejected_code: 403
          upstream: *single_fixture_upstream
    upstream: *hello_upstream
    input:
      path: /hello
    output: *hello_output

  - name: jsonpath-without-parser
    runtime: *external_runtime
    config:
      routes:
        - id: acl-external-no-parser
          uri: /hello
          plugins:
            serverless-pre-function:
              phase: access
              functions:
                - 'return function(conf, ctx) ctx.external_user = { orgs = { api7 = { team = "cloud,infra" } } } end'
            acl:
              allow_labels:
                org: [api7, apache]
                team: [cloud, infra]
              external_user_label_field: $..team
              rejected_code: 403
          upstream: *single_fixture_upstream
    upstream: *hello_upstream
    input:
      path: /hello
    output: *acl_forbidden

  - name: invalid-separator-pattern
    runtime: *external_runtime
    config:
      routes:
        - id: acl-external-invalid-pattern
          uri: /hello
          plugins:
            serverless-pre-function:
              phase: access
              functions:
                - 'return function(conf, ctx) ctx.external_user = { orgs = { api7 = { team = "cloud,infra" } } } end'
            acl:
              allow_labels:
                org: [api7, apache]
                team: [cloud, infra]
              external_user_label_field: $..team
              external_user_label_field_key: team
              external_user_label_field_parser: segmented_text
              external_user_label_field_separator: (invalid(pattern
              rejected_code: 403
          upstream: *single_fixture_upstream
    upstream: *hello_upstream
    input:
      path: /hello
    output:
      status: 403
      body:
        matches: 'The consumer is forbidden\.'
      logs:
        matches: 'failed to split labels \[cloud,infra\]'

  - name: table-parser-rejects-string
    runtime: *external_runtime
    config:
      routes:
        - id: acl-external-table-string
          uri: /hello
          plugins:
            serverless-pre-function:
              phase: access
              functions:
                - 'return function(conf, ctx) ctx.external_user = { orgs = { api7 = { team = "cloud,infra" } } } end'
            acl:
              allow_labels:
                org: [api7, apache]
                team: [cloud, infra]
              external_user_label_field: $..team
              external_user_label_field_key: team
              external_user_label_field_parser: table
              rejected_code: 403
          upstream: *single_fixture_upstream
    upstream: *hello_upstream
    input:
      path: /hello
    output:
      status: 403
      body:
        matches: 'The consumer is forbidden\.'
      logs:
        matches: 'the parser is specified as table, but the type of value is not table: string'

  - name: json-parser-rejects-table
    runtime: *external_runtime
    config:
      routes:
        - id: acl-external-json-table
          uri: /hello
          plugins:
            serverless-pre-function:
              phase: access
              functions:
                - 'return function(conf, ctx) ctx.external_user = { orgs = { api7 = { team = {"cloud", "infra"} } } } end'
            acl:
              allow_labels:
                org: [api7, apache]
                team: [cloud, infra]
              external_user_label_field: $..team
              external_user_label_field_key: team
              external_user_label_field_parser: json
              rejected_code: 403
          upstream: *single_fixture_upstream
    upstream: *hello_upstream
    input:
      path: /hello
    output:
      status: 403
      body:
        matches: 'The consumer is forbidden\.'
      logs:
        matches: 'the parser is specified as json array, but the value type is not string'

  - name: json-parser-requires-array-prefix
    runtime: *external_runtime
    config:
      routes:
        - id: acl-external-json-scalar
          uri: /hello
          plugins:
            serverless-pre-function:
              phase: access
              functions:
                - 'return function(conf, ctx) ctx.external_user = { orgs = { api7 = { team = "cloud" } } } end'
            acl:
              allow_labels:
                org: [api7, apache]
                team: [cloud, infra]
              external_user_label_field: $..team
              external_user_label_field_key: team
              external_user_label_field_parser: json
              rejected_code: 403
          upstream: *single_fixture_upstream
    upstream: *hello_upstream
    input:
      path: /hello
    output:
      status: 403
      body:
        matches: 'The consumer is forbidden\.'
      logs:
        matches: "the parser is specified as json array, but the value do not has prefix '\\['"

  - name: json-parser-rejects-invalid-json
    runtime: *external_runtime
    config:
      routes:
        - id: acl-external-json-invalid
          uri: /hello
          plugins:
            serverless-pre-function:
              phase: access
              functions:
                - 'return function(conf, ctx) ctx.external_user = { orgs = { api7 = { team = "[cloud" } } } end'
            acl:
              allow_labels:
                org: [api7, apache]
                team: [cloud, infra]
              external_user_label_field: $..team
              external_user_label_field_key: team
              external_user_label_field_parser: json
              rejected_code: 403
          upstream: *single_fixture_upstream
    upstream: *hello_upstream
    input:
      path: /hello
    output:
      status: 403
      body:
        matches: 'The consumer is forbidden\.'
      logs:
        matches: 'failed to decode labels \[\[cloud\] as array, err:'

  - name: default-parser-single-value
    runtime: *external_runtime
    config:
      routes:
        - id: acl-external-default-parser
          uri: /hello
          plugins:
            serverless-pre-function:
              phase: access
              functions:
                - 'return function(conf, ctx) ctx.external_user = { team = "cloud" } end'
            acl:
              allow_labels:
                org: [api7, apache]
                team: [cloud, infra]
              external_user_label_field: team
              rejected_code: 403
          upstream: *single_fixture_upstream
    upstream: *hello_upstream
    input:
      path: /hello
    output:
      status: 200
      body:
        equals: hello world
      logs:
        matches: 'the string value can not parsed by json or segmented_text'

  - name: multi-match-parser-deny
    runtime: *external_runtime
    config:
      routes:
        - id: acl-external-multi-match-deny
          uri: /hello
          plugins:
            serverless-pre-function:
              phase: access
              functions:
                - 'return function(conf, ctx) ctx.external_user = { teams = { { name = "cloud" }, { name = "infra,qa" } } } end'
            acl:
              deny_labels:
                name: [infra]
              external_user_label_field: $..name
              external_user_label_field_key: name
              external_user_label_field_parser: segmented_text
              external_user_label_field_separator: ','
              rejected_code: 403
          upstream: *single_fixture_upstream
    upstream: *hello_upstream
    input:
      path: /hello
    output: *acl_forbidden

  - name: mixed-type-multi-match-parser-deny
    runtime: *external_runtime
    config:
      routes:
        - id: acl-external-mixed-match-deny
          uri: /hello
          plugins:
            serverless-pre-function:
              phase: access
              functions:
                - 'return function(conf, ctx) ctx.external_user = { teams = { { name = "allowed,other" }, { name = {"blocked"} } } } end'
            acl:
              deny_labels:
                name: [blocked]
              external_user_label_field: $.teams[*].name
              external_user_label_field_key: name
              external_user_label_field_parser: segmented_text
              external_user_label_field_separator: ','
              rejected_code: 403
          upstream: *single_fixture_upstream
    upstream: *hello_upstream
    input:
      path: /hello
    output: *acl_forbidden

  - name: external-user-jsonpath-filter
    runtime: *external_runtime
    config:
      routes:
        - id: acl-external-jsonpath-filter
          uri: /hello
          plugins:
            serverless-pre-function:
              phase: access
              functions:
                - 'return function(conf, ctx) ctx.external_user = { profiles = { { score = 8, groups = {"allowed"} }, { score = 12, groups = {"blocked"} } } } end'
            acl:
              deny_labels:
                groups: [blocked]
              external_user_label_field: $.profiles[?(@.score>=10)].groups[*]
              external_user_label_field_key: groups
              rejected_code: 403
          upstream: *single_fixture_upstream
    upstream: *hello_upstream
    input:
      path: /hello
    output: *acl_forbidden

  - name: external-user-jsonpath-non-finite-fail-closed
    runtime: *external_runtime
    config:
      routes:
        - id: acl-external-jsonpath-non-finite
          uri: /hello
          plugins:
            serverless-pre-function:
              phase: access
              functions:
                - 'return function(conf, ctx) ctx.external_user = { profiles = { { score = "NaN", groups = {"edge"} } } } end'
            acl:
              allow_labels:
                groups: [edge]
              external_user_label_field: $.profiles[?(0<=@.score)].groups[*]
              external_user_label_field_key: groups
              rejected_code: 403
          upstream: *single_fixture_upstream
    upstream: *hello_upstream
    input:
      path: /hello
    output: *acl_forbidden

  - name: comma-delimited-consumer-labels
    runtime: *consumer_runtime
    config:
      routes:
        - id: acl2-comma-delimited
          uri: /hello
          plugins:
            basic-auth: {}
            acl:
              allow_labels:
                project: [apisix]
          upstream: *fixture_upstream
      consumers:
        - username: jack
          labels:
            org: apache
            project: gateway,apisix,web-server
          plugins:
            basic-auth:
              username: jack
              password: "123456"
    fixtures: [*hello_fixture]
    steps:
      - name: unauthorized
        input:
          path: /hello
        output: *missing_basic_auth
      - name: jack-allowed
        input:
          path: /hello
          headers:
            Authorization: Basic amFjazoxMjM0NTY=
        output: *hello_output
