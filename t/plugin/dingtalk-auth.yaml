sources:
- repository: https://github.com/apache/apisix
  commit: c3d7d5ec69774121f53d2e20d29d09c816795dd7
  file: t/plugin/dingtalk-auth.t
  tests: 14
cases:
- name: "dingtalk-auth-test-001-schema-check-all-required-fields-present"
  source:
    file: t/plugin/dingtalk-auth.t
    tests:
    - 1
  skip: "upstream Test::Nginx case requires Lua/Admin API setup or an external dependency; preserved as a source-complete skip until a standalone fixture is available"
- name: "dingtalk-auth-test-002-schema-check-missing-required-field-app-key"
  source:
    file: t/plugin/dingtalk-auth.t
    tests:
    - 2
  skip: "upstream Test::Nginx case requires Lua/Admin API setup or an external dependency; preserved as a source-complete skip until a standalone fixture is available"
- name: "dingtalk-auth-test-003-schema-check-secret-too-short"
  source:
    file: t/plugin/dingtalk-auth.t
    tests:
    - 3
  skip: "upstream Test::Nginx case requires Lua/Admin API setup or an external dependency; preserved as a source-complete skip until a standalone fixture is available"
- name: "dingtalk-auth-test-004-enable-dingtalk-auth-plugin"
  source:
    file: t/plugin/dingtalk-auth.t
    tests:
    - 4
  skip: "upstream Test::Nginx case requires Lua/Admin API setup or an external dependency; preserved as a source-complete skip until a standalone fixture is available"
- name: "dingtalk-auth-test-005-no-code-provided-redirect-to-redirect-uri"
  source:
    file: t/plugin/dingtalk-auth.t
    tests:
    - 5
  skip: "upstream Test::Nginx case requires Lua/Admin API setup or an external dependency; preserved as a source-complete skip until a standalone fixture is available"
- name: "dingtalk-auth-test-006-invalid-code-returns-401"
  source:
    file: t/plugin/dingtalk-auth.t
    tests:
    - 6
  skip: "upstream Test::Nginx case requires Lua/Admin API setup or an external dependency; preserved as a source-complete skip until a standalone fixture is available"
- name: "dingtalk-auth-test-007-valid-code-via-query-param-returns-200"
  source:
    file: t/plugin/dingtalk-auth.t
    tests:
    - 7
  skip: "upstream Test::Nginx case requires Lua/Admin API setup or an external dependency; preserved as a source-complete skip until a standalone fixture is available"
- name: "dingtalk-auth-test-008-valid-code-via-x-dingtalk-code-header-returns-200"
  source:
    file: t/plugin/dingtalk-auth.t
    tests:
    - 8
  skip: "upstream Test::Nginx case requires Lua/Admin API setup or an external dependency; preserved as a source-complete skip until a standalone fixture is available"
- name: "dingtalk-auth-test-009-cookie-session-subsequent-requests-reuse-session"
  source:
    file: t/plugin/dingtalk-auth.t
    tests:
    - 9
  skip: "upstream Test::Nginx case requires Lua/Admin API setup or an external dependency; preserved as a source-complete skip until a standalone fixture is available"
- name: "dingtalk-auth-test-010-cookie-expires-after-cookie-expires-in-seconds"
  source:
    file: t/plugin/dingtalk-auth.t
    tests:
    - 10
  skip: "upstream Test::Nginx case requires Lua/Admin API setup or an external dependency; preserved as a source-complete skip until a standalone fixture is available"
- name: "dingtalk-auth-test-011-configure-custom-code-header-and-code-query"
  source:
    file: t/plugin/dingtalk-auth.t
    tests:
    - 11
  skip: "upstream Test::Nginx case requires Lua/Admin API setup or an external dependency; preserved as a source-complete skip until a standalone fixture is available"
- name: "dingtalk-auth-test-012-custom-code-query-param-works"
  source:
    file: t/plugin/dingtalk-auth.t
    tests:
    - 12
  skip: "upstream Test::Nginx case requires Lua/Admin API setup or an external dependency; preserved as a source-complete skip until a standalone fixture is available"
- name: "dingtalk-auth-test-013-custom-code-header-works"
  source:
    file: t/plugin/dingtalk-auth.t
    tests:
    - 13
  skip: "upstream Test::Nginx case requires Lua/Admin API setup or an external dependency; preserved as a source-complete skip until a standalone fixture is available"
- name: "dingtalk-auth-test-014-client-supplied-x-userinfo-is-not-forwarded-to-upstream"
  source:
    file: t/plugin/dingtalk-auth.t
    tests:
    - 14
  skip: "upstream Test::Nginx case requires Lua/Admin API setup or an external dependency; preserved as a source-complete skip until a standalone fixture is available"
